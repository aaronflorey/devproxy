---
phase: 01-discovery-domains-and-conflict-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - main.go
  - cmd/devproxy/root.go
  - internal/config/config.go
  - internal/routing/types.go
autonomous: true
requirements:
  - DISC-03
  - DOMN-05
must_haves:
  truths:
    - "The codebase has one authoritative route model used by later discovery, conflict, and operator surfaces."
    - "Config defaults exist for suffix, root services, ignored services, and port preferences."
  artifacts:
    - path: "internal/routing/types.go"
      provides: "Route, route candidate, warning, and conflict contracts"
    - path: "internal/config/config.go"
      provides: "Default config and config-to-routing override structures"
    - path: "cmd/devproxy/root.go"
      provides: "CLI entrypoint and shared config bootstrap"
  key_links:
    - from: "cmd/devproxy/root.go"
      to: "internal/config/config.go"
      via: "config loader initialization"
      pattern: "config"
    - from: "internal/config/config.go"
      to: "internal/routing/types.go"
      via: "override structures reference routing contracts"
      pattern: "Route"
---

<objective>
Define the project contracts and defaults that every later Phase 1 plan will build against.

Purpose: prevent discovery, routing, and operator work from inventing incompatible data shapes.
Output: compilable Go module skeleton, shared route/config types, and CLI bootstrap.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRD.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Go module and CLI root</name>
  <files>go.mod, go.sum, main.go, cmd/devproxy/root.go</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-discovery-domains-and-conflict-policy/01-RESEARCH.md</read_first>
  <action>Create the initial Go module for `devproxy`, add Cobra as the CLI framework, add `main.go` that executes `cmd/devproxy/root.go`, and define a root command named `devproxy` with persistent config-loading hooks. The root command must expose a shared config path flag and initialize config before subcommands so later Phase 1 plans can add `routes`, `status`, and refresh behavior without rewriting startup wiring.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>go.mod declares the devproxy module; main.go calls the Cobra root command; cmd/devproxy/root.go defines a root command named `devproxy` with a persistent config setup path.</acceptance_criteria>
  <done>The repository can compile the initial binary entrypoint and future commands have a stable root bootstrap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define shared config and routing contracts</name>
  <files>internal/config/config.go, internal/routing/types.go</files>
  <read_first>.planning/PRD.md, .planning/REQUIREMENTS.md, .planning/phases/01-discovery-domains-and-conflict-policy/01-RESEARCH.md</read_first>
  <behavior>
    - Test 1: default config exposes suffix `test`, root services `app`, `web`, `nginx`, `laravel.test`, and the required ignored services / port preferences from the PRD.
    - Test 2: route contracts can represent winner metadata, losing candidates, warning records, explicit-domain DNS warnings, and label-vs-config override provenance.
  </behavior>
  <action>Create `internal/config/config.go` with typed config structs for domain suffix, root services, ignored services, ignored ports, port preference order, and per-project/service overrides per DOMN-05. Create `internal/routing/types.go` with exported structs for candidate containers, resolved routes, conflict records, warning records, and reconciliation snapshots. Include fields for Compose metadata provenance so later tasks can implement DISC-03 exactly and carry warning visibility into status/doctor/log consumers.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>internal/config/config.go contains typed defaults matching the PRD values; internal/routing/types.go exports contracts for candidate, route, warning, conflict, and snapshot data; no Phase 1 plan needs to invent new route shapes outside these files.</acceptance_criteria>
  <done>All later Phase 1 work has concrete config defaults and one shared routing vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/config file → daemon internals | Untrusted local config and operator inputs shape later routing behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | internal/config/config.go | mitigate | Use typed config structs and explicit default values so later plans validate fields instead of merging untyped maps. |
| T-01-02 | E | cmd/devproxy/root.go | accept | Phase 1 root command does not perform privileged actions yet; privilege boundaries are deferred to install/runtime phases. |
| T-01-03 | R | internal/routing/types.go | mitigate | Add provenance fields for config/label/source decisions so later warnings and conflicts are traceable. |
</threat_model>

<verification>
- `go test ./...`
- Read `internal/config/config.go` and confirm PRD defaults are encoded directly.
</verification>

<success_criteria>
- The repo has a compilable Go entrypoint.
- Shared config and route contracts exist and are reusable without reinterpretation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-01-SUMMARY.md`
</output>
