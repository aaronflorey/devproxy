---
phase: 01-discovery-domains-and-conflict-policy
plan: 03
type: tdd
wave: 2
depends_on:
  - 01
files_modified:
  - internal/routing/domains.go
  - internal/routing/overrides.go
  - internal/routing/domains_test.go
  - internal/routing/overrides_test.go
autonomous: true
requirements:
  - DOMN-01
  - DOMN-02
  - DOMN-03
  - DOMN-04
  - DOMN-05
  - DOMN-06
  - DOMN-07
must_haves:
  truths:
    - "Default routes resolve to `{service}.{project}.{suffix}` when no explicit domain overrides apply."
    - "Root-domain rules and Laravel Sail defaults produce `{project}.{suffix}` only when appropriate."
    - "Explicit domains can override inference, but public suffixes are rejected and unmanaged local suffixes emit warnings."
  artifacts:
    - path: "internal/routing/domains.go"
      provides: "Domain generation and explicit-domain validation"
    - path: "internal/routing/overrides.go"
      provides: "Field-level config + label precedence merge"
  key_links:
    - from: "internal/routing/overrides.go"
      to: "internal/routing/domains.go"
      via: "merged route options feed domain generation"
      pattern: "domain"
---

<objective>
Build the test-first domain and override engine for Phase 1 route computation.

Purpose: ensure every discovered candidate produces predictable hostnames and override behavior without hidden magic.
Output: tested domain generation, explicit-domain validation, and label-over-config merge logic.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PRD.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-RESEARCH.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for domain generation and override precedence</name>
  <files>internal/routing/domains_test.go, internal/routing/overrides_test.go</files>
  <read_first>internal/routing/types.go, internal/config/config.go, .planning/PRD.md</read_first>
  <action>Write table-driven tests covering: default domain `{service}.{project}.{suffix}`; root-domain generation for configured root services and `devproxy.root=true`; Laravel Sail handling where `laravel.test` maps to `{project}.test` and companion services keep standard subdomains; `devproxy.domain` replacing inferred domains; `devproxy.domains` adding extra domains; config overrides yielding defaults only until overlapping Docker label fields replace them; public suffixes like `.com` and `.net` being rejected; unmanaged but local-looking suffixes being allowed with a DNS warning; and malformed explicit domains being ignored field-by-field with warnings per DOMN-07.</action>
  <verify>
    <automated>go test ./internal/routing/... -run 'Test(DomainGeneration|OverridePrecedence)'</automated>
  </verify>
  <acceptance_criteria>Routing tests enumerate default domains, root rules, Sail defaults, label-over-config precedence, public suffix rejection, unmanaged-suffix warnings, and malformed-domain warning cases.</acceptance_criteria>
  <done>RED tests pin the domain policy before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement domain generation and override merging</name>
  <files>internal/routing/domains.go, internal/routing/overrides.go</files>
  <read_first>internal/routing/domains_test.go, internal/routing/overrides_test.go, internal/routing/types.go</read_first>
  <action>Implement pure routing functions that take normalized candidates plus config defaults and return resolved hostname sets, per-domain warnings, and merged route options. `domains.go` must generate default, root, Sail, explicit single-domain, and multi-domain outputs exactly per the PRD. `overrides.go` must merge config overrides and Docker label fields field-by-field, with Docker labels taking precedence for overlapping fields and malformed label values ignored without discarding valid sibling fields. Explicit domains under public suffixes must be rejected; explicit domains under unmanaged non-public suffixes must be accepted with a warning that DNS must be handled outside devproxy.</action>
  <verify>
    <automated>go test ./internal/routing/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>All routing tests pass; domain outputs include default/root/Sail/explicit forms; public suffixes are rejected; unmanaged custom suffixes remain routable with warnings.</acceptance_criteria>
  <done>Phase 1 has deterministic hostname generation and override behavior matching DOMN-01 through DOMN-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config + Docker labels → domain policy | Local config and container labels can try to claim unsafe or invalid hostnames. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | S | internal/routing/domains.go | mitigate | Reject obviously public suffixes and malformed explicit domains before they enter the active route set. |
| T-01-08 | T | internal/routing/overrides.go | mitigate | Apply field-level validation so one malformed label does not poison unrelated route fields. |
| T-01-09 | I | internal/routing/domains.go | accept | Unmanaged local suffixes are allowed by product design; mitigate confusion with explicit DNS warning records instead of silent failure. |
</threat_model>

<verification>
- `go test ./internal/routing/...`
- `go test ./...`
</verification>

<success_criteria>
- Domain generation is fully specified by tests.
- Override precedence and explicit-domain safety rules match the PRD and requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-03-SUMMARY.md`
</output>
