---
phase: 01-discovery-domains-and-conflict-policy
plan: 05
type: execute
wave: 4
depends_on:
  - 04
files_modified:
  - internal/daemon/reconciler.go
  - internal/daemon/events.go
  - internal/admin/routes.go
  - internal/admin/status.go
  - internal/admin/doctor.go
  - internal/admin/logs.go
  - internal/daemon/reconciler_test.go
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-06
must_haves:
  truths:
    - "Startup and manual refresh can build a fresh route snapshot from running containers."
    - "Docker lifecycle events update the active snapshot to match current eligible containers."
    - "Conflict and warning records are visible through status, doctor, routes, and logs read models, with structures reusable by a future dashboard."
  artifacts:
    - path: "internal/daemon/reconciler.go"
      provides: "snapshot rebuild orchestration for startup, refresh, and event-driven updates"
    - path: "internal/daemon/events.go"
      provides: "Docker event watcher contract and reconnect/resync logic"
    - path: "internal/admin/routes.go"
      provides: "route/conflict read model for operator surfaces"
    - path: "internal/admin/status.go"
      provides: "daemon health summary including last sync and watcher state"
  key_links:
    - from: "internal/daemon/reconciler.go"
      to: "internal/registry/snapshot.go"
      via: "atomic snapshot replacement"
      pattern: "Snapshot"
    - from: "internal/admin/routes.go"
      to: "internal/registry/snapshot.go"
      via: "read-only presentation of active routes and conflicts"
      pattern: "Conflict"
---

<objective>
Wire the discovery and routing logic into a daemon reconciliation flow plus operator-visible read models.

Purpose: complete Phase 1's user-visible trust loop by keeping routes current and making warnings observable everywhere they matter.
Output: startup/refresh/event reconciliation, watcher health, and shared status/routes/doctor/log projections.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PRD.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-RESEARCH.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement daemon reconciliation and watcher lifecycle</name>
  <files>internal/daemon/reconciler.go, internal/daemon/events.go, internal/daemon/reconciler_test.go</files>
  <read_first>internal/discovery/metadata.go, internal/discovery/eligibility.go, internal/routing/conflicts.go, internal/registry/snapshot.go</read_first>
  <behavior>
    - Test 1: startup and explicit refresh rebuild the snapshot from all running containers.
    - Test 2: supported Docker events (`start`, `stop`, `die`, `destroy`, `rename`, `update`) trigger targeted reconcile and publish a fresh snapshot.
    - Test 3: watcher disconnect triggers degraded health plus full re-sync on reconnect.
  </behavior>
  <action>Create a daemon reconciler that composes the discovery, domain, and conflict layers into one `RebuildSnapshot` flow for startup and refresh per DISC-01, then add an event watcher wrapper that listens for the Phase 1 Docker event set and triggers targeted recomputation per DISC-02. The watcher must record connection health, last successful sync time, and reconnect-driven full resync state so later `status`/`doctor` surfaces can expose drift or disconnect conditions explicitly instead of hiding them.</action>
  <verify>
    <automated>go test ./internal/daemon/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>Reconciler tests cover startup refresh, event-driven updates, and reconnect/full-resync behavior; daemon code updates snapshots atomically through the registry interface.</acceptance_criteria>
  <done>Route state can stay current from both full scans and live Docker events.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared routes/status/doctor/log read models</name>
  <files>internal/admin/routes.go, internal/admin/status.go, internal/admin/doctor.go, internal/admin/logs.go</files>
  <read_first>internal/registry/snapshot.go, internal/daemon/reconciler.go, .planning/ROADMAP.md, .planning/PRD.md</read_first>
  <action>Create read-only presenter/projection code for `routes`, `status`, `doctor`, and `logs` that all consume the same active snapshot plus watcher-health state. `routes.go` must expose active domains, upstream metadata, and conflict losers; `status.go` must expose daemon sync health and route counts; `doctor.go` must include conflict and unmanaged-domain warning visibility hooks for later command output; `logs.go` must shape warning/conflict events into a current-session stream model. Include fields and serialization-friendly structures that a future dashboard can consume without recomputing conflict policy, satisfying DISC-06 now and Phase 4 reuse later.</action>
  <verify>
    <automated>go test ./... </automated>
  </verify>
  <acceptance_criteria>Admin read-model files expose shared structures for routes, status, doctor, and logs; those structures include active routes, warnings, conflicts, loser candidates, and watcher health metadata.</acceptance_criteria>
  <done>Phase 1 ends with consistent operator-facing route/conflict visibility built on one authoritative source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker event stream → daemon state | External Docker lifecycle data continuously influences active route state. |
| Registry snapshot → operator surfaces | User-facing health/read models must not diverge from authoritative state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | D | internal/daemon/events.go | mitigate | Detect disconnects, mark degraded health, and force full reconcile on reconnect. |
| T-01-14 | R | internal/admin/logs.go | mitigate | Emit structured warning/conflict event records from the shared snapshot/reconcile pipeline, not ad-hoc string logs only. |
| T-01-15 | I | internal/admin/routes.go | accept | Routes and conflicts are intentionally local operator-visible data; phase scope excludes authentication because the admin surface is local-only in v1. |
</threat_model>

<verification>
- `go test ./internal/daemon/...`
- `go test ./...`
</verification>

<success_criteria>
- Snapshot rebuild works for startup, refresh, and live events.
- Status/routes/doctor/log read models expose the same warning/conflict truth.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-05-SUMMARY.md`
</output>
