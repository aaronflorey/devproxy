---
phase: 03-install-daemon-lifecycle-and-diagnostics
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - cmd/devproxy/install.go
  - internal/install/paths.go
  - internal/install/resolver.go
  - internal/install/launchd.go
  - internal/install/install.go
  - internal/install/install_test.go
autonomous: true
requirements:
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "Per D-04 and D-06, `devproxy install` stays scoped to lifecycle orchestration, creates required config/state/log paths, installs the managed resolver, bootstraps certificates, registers the daemon launchd service, and starts required services as the default baseline path."
    - "Per D-07, menu bar auto-start is installed only when `--with-menubar` is explicitly provided via a separate lifecycle path from the daemon service."
    - "Daemon and menu bar launchd roles are separated so privileged networking stays in the system daemon domain per D-08."
  artifacts:
    - path: "internal/install/install.go"
      provides: "install orchestration for paths, resolver, certificates, and launchd registration"
    - path: "internal/install/launchd.go"
      provides: "daemon vs menu bar plist generation and launchctl orchestration"
    - path: "cmd/devproxy/install.go"
      provides: "CLI install command with `--with-menubar` lifecycle option"
  key_links:
    - from: "internal/install/install.go"
      to: "internal/install/resolver.go"
      via: "managed suffix resolver file creation"
      pattern: "resolver"
    - from: "internal/install/launchd.go"
      to: "cmd/devproxy/install.go"
      via: "daemon/menu bar service registration from install flags"
      pattern: "with-menubar"
---

<objective>
Implement the macOS install and service lifecycle path for devproxy.

Purpose: satisfy the Phase 3 installation contract while preserving D-04 and D-06 through D-08 role boundaries between runtime internals and macOS lifecycle orchestration.
Output: install command plus helper packages for paths, resolver wiring, and launchd service management.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock install orchestration and launchd role behavior in tests</name>
  <files>internal/install/install_test.go</files>
  <read_first>internal/certs/mkcert.go, .planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: per D-04 and OPS-01, install creates config/state/log paths, writes `/etc/resolver/<suffix>` contents, bootstraps cert prerequisites, and starts the daemon service.
    - Test 2: per D-07, menu bar service registration occurs only when `--with-menubar` is true.
    - Test 3: per D-08, daemon plist targets the system daemon domain while menu bar plist targets the agent domain with separate labels and launchctl flows.
  </behavior>
  <action>Create installer tests that pin filesystem, resolver, mkcert bootstrap, and launchd-role expectations before implementation. The tests must explicitly cover D-06 default daemon auto-start, D-07 optional menu bar install, and D-08 daemon-versus-agent separation so the production code cannot collapse them into one service path.</action>
  <verify>
    <automated>go test ./internal/install/... -run 'TestInstall'</automated>
  </verify>
  <acceptance_criteria>Install tests assert required directories, resolver file shape, daemon service bootstrap, and menu bar opt-in behavior.</acceptance_criteria>
  <done>Install behavior is pinned before touching launchd or filesystem orchestration code.</done>
</task>

<task type="auto">
  <name>Task 2: Implement install helpers and the install command</name>
  <files>cmd/devproxy/install.go, internal/install/paths.go, internal/install/resolver.go, internal/install/launchd.go, internal/install/install.go</files>
  <read_first>internal/install/install_test.go, cmd/devproxy/commands.go, internal/certs/mkcert.go, .planning/ROADMAP.md</read_first>
  <action>Implement installer helpers for path creation, resolver file materialization, and launchd plist/launchctl orchestration, then add `devproxy install` as the CLI entrypoint. Per D-04, keep this layer focused on lifecycle orchestration only; do not move reconcile or proxy logic here. Per D-06 through D-08, default install must register and start the core daemon in the system daemon domain, and `--with-menubar` must additionally install a separate menu bar agent without becoming the default path.</action>
  <verify>
    <automated>go test ./internal/install/... ./cmd/devproxy/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`devproxy install` exists, the resolver file format matches `domain`, `nameserver 127.0.0.1`, and `port 53535`, and launchd helpers clearly separate daemon and optional menu bar service registration.</acceptance_criteria>
  <done>Developers can install and start devproxy on macOS without manual resolver or launchd setup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI install command → filesystem and launchd | The installer writes privileged local files and registers long-running services. |
| Install helpers → mkcert / launchctl / resolver state | External OS tools and files determine whether devproxy is safely installed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | internal/install/resolver.go | mitigate | Write only the managed `/etc/resolver/<suffix>` file contents with explicit loopback nameserver/port values instead of mutating global resolver config. |
| T-03-08 | E | internal/install/launchd.go | mitigate | Separate system-daemon and user-agent plist generation so privileged listeners stay in the daemon domain per D-08. |
| T-03-09 | R | internal/install/install.go | mitigate | Make install steps explicit and testable so daemon/menu bar service creation and cert bootstrap choices are observable per D-04/D-07. |
</threat_model>

<verification>
- `go test ./internal/install/...`
- `go test ./cmd/devproxy/...`
- `go test ./...`
</verification>

<success_criteria>
- `devproxy install` performs the full baseline daemon install flow.
- Menu bar auto-start remains opt-in and separately managed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-install-daemon-lifecycle-and-diagnostics-03-SUMMARY.md`
</output>
