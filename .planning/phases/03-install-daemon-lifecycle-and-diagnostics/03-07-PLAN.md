---
phase: 03-install-daemon-lifecycle-and-diagnostics
plan: 07
type: execute
wave: 2
depends_on:
  - 03-05
files_modified:
  - internal/install/launchd.go
  - internal/install/launchd_test.go
  - internal/install/uninstall.go
  - internal/install/uninstall_test.go
autonomous: true
gap_closure: true
requirements:
  - OPS-08
must_haves:
  truths:
    - "Per D-05, `devproxy uninstall` continues into resolver removal and the selected cleanup scope when `launchctl bootout` hits the observed absent-service `Boot-out failed: 5: Input/output error` path."
    - "Per D-04 and D-08, launchd teardown still fails loudly for real bootout problems that are not proven to be already-removed service state."
    - "The uninstall regression is locked in tests with the exact UAT bootout error string so future launchd matcher changes cannot reintroduce the abort-before-cleanup behavior."
  artifacts:
    - path: "internal/install/launchd.go"
      provides: "safe bootout fallback that distinguishes absent-service bootout noise from real launchctl failures"
    - path: "internal/install/uninstall.go"
      provides: "uninstall orchestration that preserves teardown-first flow while accepting verified idempotent launchd states"
    - path: "internal/install/launchd_test.go"
      provides: "unit coverage for bootout fallback and non-missing-state preservation"
    - path: "internal/install/uninstall_test.go"
      provides: "UAT regression coverage proving scoped cleanup still runs after the bootout exit-5 path"
  key_links:
    - from: "internal/install/uninstall.go"
      to: "internal/install/launchd.go"
      via: "stop-service error classification before resolver/scoped cleanup"
      pattern: "Boot-out failed: 5|Input/output error|launchctl print"
    - from: "internal/install/launchd.go"
      to: "launchctl print {domain}/{label}"
      via: "fallback probe used only after bootout exit-5 missing-state detection"
      pattern: "print system/|print gui/"
---

<objective>
Close the remaining Phase 3 uninstall regression from 03-VERIFICATION.md.

Purpose: make selective uninstall cleanup survive the real macOS `launchctl bootout ... exit status 5` absent-service path without hiding genuine launchd failures.
Output: bootout-aware launchd teardown logic plus regression tests for the exact UAT error.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-VERIFICATION.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-HUMAN-UAT.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-install-daemon-lifecycle-and-diagnostics-04-SUMMARY.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-install-daemon-lifecycle-and-diagnostics-05-SUMMARY.md

<interfaces>
From internal/install/uninstall.go:
```go
type UninstallDependencies struct {
	CurrentEUID            func() int
	StopDaemonService      func(context.Context, LaunchdServiceConfig) error
	UninstallDaemonService func(context.Context, LaunchdServiceConfig) error
	RemoveResolver         func(context.Context, ResolverConfig) error
}

func (u *Uninstaller) Uninstall(ctx context.Context, opts UninstallOptions) error
```

From internal/install/launchd.go:
```go
type LaunchdServiceConfig struct {
	Label     string
	Domain    LaunchdDomain
	PlistPath string
	Program   string
	Arguments []string
}

func StopService(ctx context.Context, cfg LaunchdServiceConfig) error
func UninstallService(ctx context.Context, cfg LaunchdServiceConfig) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the bootout exit-5 uninstall regression in tests</name>
  <files>internal/install/launchd_test.go, internal/install/uninstall_test.go</files>
  <read_first>internal/install/launchd.go, internal/install/uninstall.go, .planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-VERIFICATION.md, .planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-HUMAN-UAT.md</read_first>
  <behavior>
    - Test 1: per D-05, uninstall given the exact UAT daemon-stop error `launchctl bootout system /Library/LaunchDaemons/com.devproxy.daemon.plist failed: exit status 5: Boot-out failed: 5: Input/output error` still reaches `RemoveResolver` and the selected cleanup functions.
    - Test 2: per D-04 and D-08, `StopService` treats bootout exit 5 as success only when a follow-up missing-state probe proves the service is already absent.
    - Test 3: non-missing-state bootout failures still return the original launchctl error and keep fail-loud behavior intact.
  </behavior>
  <action>Add focused regressions for the remaining verification gap. In `internal/install/uninstall_test.go`, reproduce the exact UAT bootout error string from 03-VERIFICATION.md and assert uninstall continues to resolver removal plus the chosen cleanup scope instead of returning `stop daemon service: ...`. In new `internal/install/launchd_test.go`, stub the launchctl execution path so one case simulates `bootout` failing with `Boot-out failed: 5: Input/output error` followed by `launchctl print system/com.devproxy.daemon` reporting `Could not find service`, and a second case proves unrelated bootout failures are still surfaced verbatim.</action>
  <verify>
    <automated>go test ./internal/install/... -run 'Test(Uninstall|StopService)'</automated>
  </verify>
  <acceptance_criteria>`internal/install/uninstall_test.go` contains the exact UAT bootout error text, `internal/install/launchd_test.go` exists, and the tests prove both continued cleanup and preservation of real launchctl failures.</acceptance_criteria>
  <done>The observed macOS uninstall failure is reproducible in automated tests before code changes begin.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bootout fallback without masking real launchd failures</name>
  <files>internal/install/launchd.go, internal/install/launchd_test.go, internal/install/uninstall.go, internal/install/uninstall_test.go</files>
  <read_first>internal/install/launchd_test.go, internal/install/uninstall_test.go, internal/install/launchd.go, internal/install/uninstall.go</read_first>
  <action>Update `internal/install/launchd.go` so `StopService` handles the UAT bootout path safely: when `launchctl bootout` returns the exact missing-state-shaped fragment `Boot-out failed: 5: Input/output error`, run a follow-up `launchctl print {domain}/{label}` probe (`system/com.devproxy.daemon` for the daemon, `gui/<uid>/com.devproxy.menubar` for the menubar) and treat the error as idempotent success only if that probe reports a known missing-service state (`Could not find service`, `service already unloaded`, `no such process`, or `no such file`). Keep every other bootout failure fatal. Update `internal/install/uninstall.go` to reuse the same known-missing-state recognition for dependency-injected teardown errors so the exact UAT wrapper string also allows uninstall to proceed per D-05, then preserve the existing order: stop/uninstall services, remove resolver, run only the cleanup categories the user selected.</action>
  <verify>
    <automated>go test ./internal/install/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`internal/install/launchd.go` contains a `launchctl print` fallback for the `Boot-out failed: 5: Input/output error` path, `internal/install/uninstall.go` accepts the exact verified missing-state wrapper, and `go test ./internal/install/... && go test ./...` exits 0.</acceptance_criteria>
  <done>`devproxy uninstall` no longer aborts before scoped cleanup on the verified absent-service bootout path, while unrelated launchd errors still fail loudly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uninstall flow → launchctl | Local operator commands act on privileged launchd state that may already be partially removed. |
| launchctl error text → cleanup continuation | Untrusted OS error strings determine whether destructive cleanup proceeds or aborts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | D | internal/install/uninstall.go | mitigate | Continue uninstall only after matching the verified absent-service bootout path so selected cleanup is not blocked by idempotent teardown noise. |
| T-03-20 | T | internal/install/launchd.go | mitigate | Require a follow-up `launchctl print {domain}/{label}` missing-state probe before suppressing the `Boot-out failed: 5: Input/output error` case, preserving real launchd failures. |
| T-03-21 | E | internal/install/launchd.go | accept | The code already runs under root-only lifecycle commands; this plan preserves the root gate and does not expand privilege boundaries. |
</threat_model>

<verification>
- `go test ./internal/install/... -run 'Test(Uninstall|StopService)'`
- `go test ./internal/install/...`
- `go test ./...`
</verification>

<success_criteria>
- The exact UAT `launchctl bootout ... Boot-out failed: 5: Input/output error` path is covered by automated tests and no longer aborts uninstall before scoped cleanup.
- Real bootout failures that are not proven missing-state conditions still surface as fatal errors.
</success_criteria>

<output>
After completion, create `.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-install-daemon-lifecycle-and-diagnostics-07-SUMMARY.md`
</output>
