---
phase: 02-local-dns-proxy-and-https-serving
plan: 05
type: execute
wave: 4
depends_on:
  - 02
  - 03
  - 04
files_modified:
  - internal/proxy/https.go
  - internal/proxy/https_test.go
  - internal/daemon/network.go
  - internal/admin/status.go
  - internal/admin/doctor.go
autonomous: true
requirements:
  - NET-03
  - NET-04
  - NET-05
must_haves:
  truths:
    - "The daemon can run HTTP on port 80 and HTTPS on port 443 using the prepared routing and certificate state."
    - "HTTPS uses locally trusted certificates selected from the active hostname inventory."
    - "Operator-facing health data includes DNS, HTTP, HTTPS, pause state, and certificate readiness from the same daemon-owned runtime."
  artifacts:
    - path: "internal/proxy/https.go"
      provides: "HTTPS listener wiring and certificate selection over the shared proxy handler"
    - path: "internal/daemon/network.go"
      provides: "listener lifecycle, pause-aware runtime assembly, and health state"
    - path: "internal/admin/status.go"
      provides: "network runtime status projection for future CLI and dashboard surfaces"
  key_links:
    - from: "internal/proxy/https.go"
      to: "internal/certs/store.go"
      via: "certificate lookup and tls.Config"
      pattern: "Certificate"
    - from: "internal/daemon/network.go"
      to: "internal/proxy/http.go"
      via: "shared managed-host request handling"
      pattern: "Handler"
---

<objective>
Wire HTTPS serving and runtime health into the daemon so the full Phase 2 data plane is runnable and observable.

Purpose: finish the phase by assembling DNS, HTTP, HTTPS, pause state, and certificate readiness into one daemon-owned runtime.
Output: HTTPS listener code, runtime assembly, and admin health projections for the new serving components.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-CONTEXT.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-RESEARCH.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-03-PLAN.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HTTPS listener tests and runtime health expectations</name>
  <files>internal/proxy/https_test.go</files>
  <read_first>internal/proxy/http.go, internal/certs/store.go, internal/admin/status.go, .planning/REQUIREMENTS.md</read_first>
  <action>Create tests that verify HTTPS listener construction uses a `tls.Config` backed by the prepared certificate inventory, that managed active routes can resolve a certificate for their Hostname, that HTTP and HTTPS listeners can share the same proxy/no-route/paused handler behavior, and that runtime status structures can represent DNS, HTTP, HTTPS, paused, and certificate-ready health independently.</action>
  <verify>
    <automated>go test ./internal/proxy/... -run 'TestHTTPS' && go test ./internal/admin/... -run 'TestStatus'</automated>
  </verify>
  <acceptance_criteria>HTTPS tests assert tls-backed listener setup and managed-host certificate selection; status or admin tests assert health fields for DNS, HTTP, HTTPS, pause state, and certificate readiness.</acceptance_criteria>
  <done>Runtime and HTTPS expectations are locked before final listener assembly work.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HTTPS listener assembly and network runtime projections</name>
  <files>internal/proxy/https.go, internal/daemon/network.go, internal/admin/status.go, internal/admin/doctor.go</files>
  <read_first>internal/proxy/https_test.go, internal/admin/status.go, internal/admin/doctor.go, internal/proxy/http.go, internal/certs/mkcert.go</read_first>
  <action>Create `internal/proxy/https.go` with HTTPS listener setup that reuses the HTTP proxy decision path but serves through a `tls.Config` backed by the active certificate inventory. Create `internal/daemon/network.go` to assemble DNS, HTTP, and HTTPS runtime components around the reconciler snapshot reader and paused-routing state, with explicit listener health fields for bind success, cert readiness, and managed suffix configuration. Update `internal/admin/status.go` and `internal/admin/doctor.go` so later operator commands can report DNS/proxy/cert runtime truth from this single daemon-owned network runtime.</action>
  <verify>
    <automated>go test ./internal/proxy/... ./internal/admin/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`internal/proxy/https.go` and `internal/daemon/network.go` exist; tests pass for HTTPS listener setup; admin status/doctor structures expose DNS, HTTP, HTTPS, pause, and certificate readiness data for future CLI use.</acceptance_criteria>
  <done>Phase 2 finishes with a runnable local DNS/HTTP/HTTPS serving runtime and shared health projections for the next operations phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prepared cert/runtime state → bound local listeners | The daemon turns reconciled state into privileged local listeners on the developer machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | D | internal/daemon/network.go | mitigate | Track bind failures and cert readiness explicitly so startup can fail loudly instead of partially serving traffic. |
| T-02-13 | I | internal/proxy/https.go | accept | HTTPS is intentionally local-only on trusted loopback; authentication remains outside v1 scope. |
| T-02-14 | R | internal/admin/doctor.go | mitigate | Expose network runtime and certificate readiness through doctor/status structures so future diagnostics can explain HTTPS failures. |
</threat_model>

<verification>
- `go test ./internal/proxy/... ./internal/admin/...`
- `go test ./...`
</verification>

<success_criteria>
- The daemon has one runtime assembly for DNS, HTTP, HTTPS, and pause state.
- Operator-facing health projections are ready for Phase 3 status and doctor commands.
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-dns-proxy-and-https-serving/02-local-dns-proxy-and-https-serving-05-SUMMARY.md`
</output>
