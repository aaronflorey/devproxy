---
phase: 02-local-dns-proxy-and-https-serving
plan: 03
type: tdd
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - internal/proxy/http.go
  - internal/proxy/http_test.go
  - internal/admin/routes.go
  - internal/admin/logs.go
autonomous: true
requirements:
  - NET-02
  - NET-05
  - NET-06
  - NET-07
  - NET-08
must_haves:
  truths:
    - "HTTP requests for active managed routes proxy to the reconciled upstream host, port, and scheme."
    - "Requests for managed hostnames without an active route receive a friendly local no-route response."
    - "Paused routing produces a distinct friendly local response while leaving DNS and route metadata intact."
  artifacts:
    - path: "internal/proxy/http.go"
      provides: "HTTP listener handler, reverse proxy wiring, and friendly fallback responses"
    - path: "internal/proxy/http_test.go"
      provides: "request-path, upstream, and WebSocket upgrade coverage"
    - path: "internal/admin/routes.go"
      provides: "route view fields for effective upstream and HTTPS/redirect visibility"
  key_links:
    - from: "internal/proxy/http.go"
      to: "internal/dns/server.go"
      via: "managed hostname classification"
      pattern: "managed"
    - from: "internal/proxy/http.go"
      to: "internal/routing/types.go"
      via: "upstream target selection"
      pattern: "Upstream"
---

<objective>
Implement the HTTP serving path, including proxying active routes and returning explicit local responses for managed misses or paused routing.

Purpose: satisfy the main request flow before HTTPS-specific certificate and listener work is added.
Output: tested HTTP proxy handlers with WebSocket-safe reverse proxy behavior.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-CONTEXT.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-RESEARCH.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-01-PLAN.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for HTTP proxy, no-route, paused, and upgrade behavior</name>
  <files>internal/proxy/http_test.go</files>
  <read_first>internal/admin/routes.go, internal/admin/logs.go, internal/routing/types.go, .planning/phases/02-local-dns-proxy-and-https-serving/02-CONTEXT.md</read_first>
  <action>Create tests that send HTTP requests with managed Host headers and cover these exact outcomes: an active route proxies to the effective upstream host/port and preserves the configured upstream scheme; a managed hostname missing from the snapshot returns a friendly local no-route response; paused routing returns a distinct friendly paused response for the same managed hostname; an unmanaged Host header is not claimed by devproxy's friendly local handlers; and reverse proxy setup preserves upgrade-capable behavior needed for WebSocket traffic.</action>
  <verify>
    <automated>go test ./internal/proxy/... -run 'Test(HTTPProxy|NoRoute|Paused|WebSocket)'</automated>
  </verify>
  <acceptance_criteria>proxy tests include active-route proxying, managed no-route, managed paused, unmanaged-host bypass behavior, and WebSocket-safe proxy coverage.</acceptance_criteria>
  <done>RED tests lock the HTTP serving semantics for NET-02, NET-05, NET-06, NET-07, and NET-08.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the HTTP proxy handler and serving projections</name>
  <files>internal/proxy/http.go, internal/admin/routes.go, internal/admin/logs.go</files>
  <read_first>internal/proxy/http_test.go, internal/admin/routes.go, internal/admin/logs.go, internal/dns/server.go, internal/routing/types.go</read_first>
  <action>Create `internal/proxy/http.go` with an HTTP handler that uses managed-host helpers plus the published snapshot and paused state to decide between reverse proxying, friendly no-route, and friendly paused responses. Use `httputil.ReverseProxy` configured from the reconciled `routing.Route.Upstream` values and preserve default upgrade behavior for WebSocket traffic. Update `internal/admin/routes.go` and `internal/admin/logs.go` so route and runtime projections include effective upstream scheme/port plus enough response-state detail for later operator diagnostics.</action>
  <verify>
    <automated>go test ./internal/proxy/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`internal/proxy/http.go` exists and uses `httputil.ReverseProxy`; HTTP tests pass for proxy/no-route/paused cases; route or log views expose effective upstream and request-handling state useful to later status surfaces.</acceptance_criteria>
  <done>HTTP traffic for managed domains is either proxied correctly or terminated locally with clear friendly behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Incoming HTTP requests → local proxy handler | Arbitrary local requests choose a Host header and can attempt upgrade or fallback paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | S | internal/proxy/http.go | mitigate | Only claim friendly fallback responses for hostnames under the configured managed suffix. |
| T-02-07 | T | internal/proxy/http.go | mitigate | Build reverse-proxy targets strictly from reconciled upstream metadata, not from request headers. |
| T-02-08 | D | internal/proxy/http.go | mitigate | Preserve reverse-proxy upgrade behavior so WebSocket traffic does not dead-end through wrapper middleware. |
</threat_model>

<verification>
- `go test ./internal/proxy/...`
- `go test ./...`
</verification>

<success_criteria>
- Active routes proxy over HTTP using reconciled upstream metadata.
- Managed no-route and paused cases produce clear local responses instead of generic failures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-dns-proxy-and-https-serving/02-local-dns-proxy-and-https-serving-03-SUMMARY.md`
</output>
