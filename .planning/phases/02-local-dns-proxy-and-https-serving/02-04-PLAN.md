---
phase: 02-local-dns-proxy-and-https-serving
plan: 04
type: tdd
wave: 3
depends_on:
  - 01
  - 03
files_modified:
  - internal/certs/store.go
  - internal/certs/store_test.go
  - internal/certs/mkcert.go
  - internal/certs/mkcert_test.go
autonomous: true
requirements:
  - NET-03
  - NET-04
must_haves:
  truths:
    - "Certificate management derives its hostname inventory from winning routes in the published snapshot."
    - "Certificates are reused when existing project root plus wildcard coverage still matches served hostnames."
    - "mkcert execution failures surface explicitly instead of being silently ignored."
  artifacts:
    - path: "internal/certs/store.go"
      provides: "hostname inventory grouping, coverage diffing, and certificate cache metadata"
    - path: "internal/certs/mkcert.go"
      provides: "mkcert invocation wrapper and loadable leaf certificate output"
  key_links:
    - from: "internal/certs/store.go"
      to: "internal/routing/types.go"
      via: "served hostname inventory derivation"
      pattern: "Hostname"
---

<objective>
Implement certificate inventory management and mkcert-backed leaf issuance for served hostnames.

Purpose: make HTTPS trustworthy without reissuing certificates on every routine route churn.
Output: tested hostname grouping, coverage diffing, and mkcert execution wrappers.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-CONTEXT.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-RESEARCH.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for hostname grouping and certificate reuse</name>
  <files>internal/certs/store_test.go, internal/certs/mkcert_test.go</files>
  <read_first>internal/routing/types.go, .planning/PROJECT.md, .planning/phases/02-local-dns-proxy-and-https-serving/02-CONTEXT.md</read_first>
  <action>Add tests that cover these exact certificate rules: winning routes under one project base hostname produce one certificate unit covering `{project}.{suffix}` plus `*.{project}.{suffix}` when every served hostname is one label below the project root; adding a new project base hostname requires a new certificate unit; adding a deeper hostname shape outside wildcard coverage forces reissue or additional coverage; and mkcert command failures return explicit errors instead of empty certificates.</action>
  <verify>
    <automated>go test ./internal/certs/... -run 'Test(CertificateInventory|MKCert)'</automated>
  </verify>
  <acceptance_criteria>cert tests include reuse, new-project issuance, deeper-hostname reissue, and explicit mkcert failure handling.</acceptance_criteria>
  <done>RED tests define the certificate lifecycle required by NET-03 and NET-04.</done>
</task>

<task type="auto">
  <name>Task 2: Implement certificate inventory and mkcert issuance helpers</name>
  <files>internal/certs/store.go, internal/certs/mkcert.go</files>
  <read_first>internal/certs/store_test.go, internal/certs/mkcert_test.go, internal/routing/types.go, .planning/PRD.md</read_first>
  <action>Create `internal/certs/store.go` with types and helpers that derive active served hostnames from winning snapshot routes, group hostnames by project base domain, detect whether existing root plus wildcard coverage is still sufficient, and return the exact SAN set needed when reissue is required. Create `internal/certs/mkcert.go` with a thin wrapper that invokes `mkcert` for the derived SAN list, stores output paths or PEM material in a cache-friendly structure, and returns explicit errors when `mkcert` is missing or issuance fails.</action>
  <verify>
    <automated>go test ./internal/certs/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`internal/certs/store.go` and `internal/certs/mkcert.go` exist; tests prove project wildcard reuse behavior; mkcert wrapper returns explicit errors for missing or failed issuance.</acceptance_criteria>
  <done>HTTPS certificate material can be prepared deterministically from the active route inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Active hostname inventory → certificate issuance | The daemon turns local route state into trusted TLS material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | T | internal/certs/store.go | mitigate | Derive SANs only from winning managed hostnames so stale or losing routes do not leak into certificates. |
| T-02-10 | D | internal/certs/mkcert.go | mitigate | Surface mkcert failures directly so HTTPS startup fails clearly instead of serving broken TLS. |
| T-02-11 | R | internal/certs/store.go | mitigate | Persist or expose certificate coverage metadata so reissue decisions are explainable later in diagnostics. |
</threat_model>

<verification>
- `go test ./internal/certs/...`
- `go test ./...`
</verification>

<success_criteria>
- Certificate issuance is derived from active served hostnames.
- Reuse vs reissue decisions are deterministic and testable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-dns-proxy-and-https-serving/02-local-dns-proxy-and-https-serving-04-SUMMARY.md`
</output>
