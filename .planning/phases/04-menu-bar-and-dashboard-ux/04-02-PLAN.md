---
phase: 04-menu-bar-and-dashboard-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - cmd/devproxy/dashboard.go
  - internal/dashboard/server.go
  - internal/dashboard/server_test.go
  - internal/dashboard/templates.go
  - internal/dashboard/templates/dashboard.html.tmpl
  - internal/dashboard/templates/logs.html.tmpl
  - internal/dashboard/static/dashboard.css
autonomous: true
requirements:
  - UI-04
  - UI-03
must_haves:
  truths:
    - "Developer can open a local dashboard that shows daemon health, active routes, recent conflicts, and recent current-session errors, per D-05."
    - "Dashboard content comes from admin API projections instead of direct daemon internals, per D-01."
    - "Route links and refresh/log views use the daemon-provided open metadata and session-scoped logs, per D-04 and D-05."
  artifacts:
    - path: "cmd/devproxy/dashboard.go"
      provides: "Dashboard command and localhost server entrypoint"
    - path: "internal/dashboard/server.go"
      provides: "HTTP handlers and admin-client-backed view-model assembly"
    - path: "internal/dashboard/templates/dashboard.html.tmpl"
      provides: "Main dashboard UI contract"
    - path: "internal/dashboard/templates/logs.html.tmpl"
      provides: "Session log/error view used by menu bar Open Logs action"
  key_links:
    - from: "internal/dashboard/server.go"
      to: "internal/adminapi/client.go"
      via: "status/routes/logs/startup client calls"
      pattern: "Status\(|Routes\(|Logs\(|Doctor\(|Refresh\("
    - from: "cmd/devproxy/dashboard.go"
      to: "internal/dashboard/server.go"
      via: "fixed localhost listen address and browser-open flow"
      pattern: "127\.0\.0\.1|localhost|Open"
---

<objective>
Build the local dashboard HTTP surface as a thin localhost client over the admin API.

Purpose: satisfy UI-04 before the menu bar runtime consumes it, and give menu actions concrete dashboard/log URLs to open.
Output: `devproxy dashboard` command, localhost HTTP server, server-rendered dashboard/log pages, and styling that follows the approved UI contract.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-CONTEXT.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-RESEARCH.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-UI-SPEC.md
@internal/adminapi/client.go
@internal/adminapi/types.go
@cmd/devproxy/root.go
@cmd/devproxy/commands.go
@cmd/devproxy/logs.go

<interfaces>
From internal/adminapi/client.go:
```go
func (c *Client) Status(ctx context.Context) (admin.StatusView, error)
func (c *Client) Routes(ctx context.Context) ([]admin.RouteView, error)
func (c *Client) Logs(ctx context.Context) ([]admin.LogEvent, error)
func (c *Client) Doctor(ctx context.Context) (admin.DoctorView, error)
func (c *Client) Refresh(ctx context.Context, reason string) (RefreshResponse, error)
```

From .planning/phases/04-menu-bar-and-dashboard-ux/04-UI-SPEC.md:
```text
Background #0F172A, cards #1E293B, accent #22C55E, destructive #DC2626.
Spacing tokens: 4/8/16/24/32/48/64 px.
Copy: "Refresh Routes", "No Active Routes", and the approved error-state strings.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock dashboard handlers, page sections, and localhost behavior in tests</name>
  <files>internal/dashboard/server_test.go</files>
  <read_first>internal/adminapi/client.go, internal/adminapi/types.go, cmd/devproxy/logs.go, .planning/phases/04-menu-bar-and-dashboard-ux/04-UI-SPEC.md</read_first>
  <behavior>
    - Test 1: `GET /` renders daemon health, active routes, recent conflicts, and recent session errors from admin API responses.
    - Test 2: `GET /logs` renders current-session log/error data and preserves session-scoped wording from D-05.
    - Test 3: `POST /actions/refresh` calls the admin API refresh path and redirects back to `/` with success/failure flash text.
    - Test 4: route links in rendered HTML use the daemon-provided `OpenURL` values rather than rebuilding URLs in templates.
  </behavior>
  <action>Write RED tests for a dedicated `internal/dashboard` package. Require a fixed localhost default such as `127.0.0.1:45831`, a main dashboard route `/`, a logs route `/logs`, and a refresh action route `/actions/refresh`. Assert the rendered HTML contains the exact UI-SPEC copy strings `Refresh Routes`, `No Active Routes`, and `DevProxy can’t reach the daemon right now. Ensure the daemon is running, then select Run Doctor for repair guidance.` Assert route rows render anchor `href` values from `OpenURL` fields provided by the admin client.</action>
  <verify>
    <automated>go test ./internal/dashboard/... -run 'TestDashboard'</automated>
  </verify>
  <acceptance_criteria>`internal/dashboard/server_test.go` contains route coverage for `/`, `/logs`, and `/actions/refresh`; tests grep for `Refresh Routes`, `No Active Routes`, and `href=` assertions using `OpenURL` values.</acceptance_criteria>
  <done>Dashboard HTTP behavior is pinned before implementation and menubar plan 03 can rely on stable URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the dashboard command, server-rendered pages, and styling</name>
  <files>cmd/devproxy/dashboard.go, internal/dashboard/server.go, internal/dashboard/templates.go, internal/dashboard/templates/dashboard.html.tmpl, internal/dashboard/templates/logs.html.tmpl, internal/dashboard/static/dashboard.css</files>
  <read_first>internal/dashboard/server_test.go, cmd/devproxy/commands.go, cmd/devproxy/root.go, .planning/phases/04-menu-bar-and-dashboard-ux/04-UI-SPEC.md</read_first>
  <action>Implement `devproxy dashboard` as a Cobra subcommand that starts a localhost HTTP server on `127.0.0.1:45831` by default and can optionally open the browser after startup. In `internal/dashboard/server.go`, use `internal/adminapi.Client` as the only data source; fetch status, routes, logs, and doctor/startup data as needed; expose handlers for `/`, `/logs`, and `POST /actions/refresh`; and surface daemon-unreachable failures with the exact approved error-state copy. In the templates and CSS, apply the UI-SPEC tokens directly: background `#0F172A`, card surfaces `#1E293B`, accent `#22C55E`, destructive `#DC2626`, and spacing tokens `4/8/16/24/32/48/64px`. Render sections for daemon health, pause/startup status summary, active routes, recent conflicts, and recent current-session errors only; do not add persisted history or preferences per D-05.</action>
  <verify>
    <automated>go test ./internal/dashboard/... ./cmd/devproxy/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`cmd/devproxy/dashboard.go` registers a `dashboard` command; `internal/dashboard/server.go` serves `/`, `/logs`, and `/actions/refresh`; template/static files contain the specified copy and color tokens from the UI-SPEC; no dashboard code imports `internal/daemon` directly.</acceptance_criteria>
  <done>The dashboard is locally openable, thin over admin API data, and ready for the menubar launch action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> localhost dashboard server | Untrusted HTTP requests hit the local dashboard surface |
| dashboard server -> admin API | Dashboard process consumes privileged local daemon projections/actions |
| dashboard HTML -> browser open links | Rendered route links direct the browser to local managed hostnames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | S | `internal/dashboard/server.go` | mitigate | Bind only to `127.0.0.1` by default and reject non-local listen defaults in tests/config handling. |
| T-04-06 | T | refresh action handler | mitigate | Accept only POST on `/actions/refresh` and hardcode the admin API action instead of proxying arbitrary paths. |
| T-04-07 | I | templates/log view | mitigate | Escape all rendered strings via `html/template`; never mark admin messages as trusted HTML. |
| T-04-08 | D | dashboard server | mitigate | Keep pages read-mostly, bounded by existing admin payload limits; render only current-session issue slices rather than unbounded history. |
</threat_model>

<verification>
- `go test ./internal/dashboard/... ./cmd/devproxy/...`
- `go test ./...`
</verification>

<success_criteria>
- `devproxy dashboard` serves a localhost page showing daemon health, routes, recent conflicts, and current-session errors.
- The page styling and copy match the approved UI contract tokens.
- Dashboard links and actions use the admin API contracts introduced in plan 01, not direct daemon/package access.
</success_criteria>

<output>
After completion, create `.planning/phases/04-menu-bar-and-dashboard-ux/04-menu-bar-and-dashboard-ux-02-SUMMARY.md`
</output>
