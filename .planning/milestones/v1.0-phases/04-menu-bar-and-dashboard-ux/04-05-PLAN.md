---
phase: 04-menu-bar-and-dashboard-ux
plan: "05"
type: execute
wave: 5
depends_on: ["04-03", "04-04"]
files_modified:
  - internal/menubar/runtime_darwin.go
  - internal/menubar/runtime_darwin_test.go
  - internal/menubar/app_test.go
autonomous: false
gap_closure: true
requirements: [UI-03]
must_haves:
  truths:
    - "Developer can see active routes as selectable menu bar items."
    - "Selecting a route opens the daemon-provided OpenURL, preserving https/http fallback behavior per D-04."
    - "Route menu wiring is covered by runtime-level tests, not dispatcher-only tests."
  artifacts:
    - path: internal/menubar/runtime_darwin.go
      provides: "Dynamic systray route-item rendering and click binding"
      contains: "dispatcher.openRoute"
    - path: internal/menubar/runtime_darwin_test.go
      provides: "Runtime-level coverage for route slot sync and click dispatch"
      contains: "Test"
    - path: internal/menubar/app_test.go
      provides: "Menu-state regression coverage for route item projection"
      contains: "RouteItems"
  key_links:
    - from: internal/menubar/app.go
      to: internal/menubar/runtime_darwin.go
      via: buildMenuState().RouteItems
      pattern: "RouteItems"
    - from: internal/menubar/runtime_darwin.go
      to: internal/menubar/open.go
      via: dispatcher.openRoute(route.OpenURL)
      pattern: "openRoute"
---

<objective>
Close the Phase 4 verification blocker by making daemon-projected routes reachable from the native macOS systray runtime.

Purpose: satisfy UI-03 and the failed verification truth without introducing a second control plane.
Output: runtime route-item wiring, runtime-level tests, and a final native UX verification checkpoint.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-CONTEXT.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-RESEARCH.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-VERIFICATION.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-UI-SPEC.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-menu-bar-and-dashboard-ux-03-SUMMARY.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-menu-bar-and-dashboard-ux-04-SUMMARY.md
@internal/menubar/app.go
@internal/menubar/runtime_darwin.go
@internal/menubar/app_test.go
@internal/menubar/open.go

<interfaces>
From `internal/menubar/app.go`:

```go
type routeMenuItem struct {
	Hostname string
	OpenURL  string
}

type menuState struct {
	HealthLine       string
	PauseLine        string
	ActiveRoutesLine string
	StartupLine      string
	ErrorLine        string
	RouteItems       []routeMenuItem
	RepairActions    repairActions
}

func buildMenuState(status admin.StatusView, routes []admin.RouteView, startup []adminapi.StartupRoleStatus) menuState

func (d *dispatcher) openRoute(ctx context.Context, routeURL string) error
```

From `internal/menubar/runtime_darwin.go`:

```go
func Run(ctx context.Context, client adminClient, op opener) error
func refreshMenu(ctx context.Context, client adminClient, statusItem, pauseItem, startupItem *systray.MenuItem) error
```

From `internal/menubar/open.go`:

```go
func (o *defaultOpener) OpenURL(ctx context.Context, target string) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the missing route-open runtime behavior in tests</name>
  <files>internal/menubar/runtime_darwin_test.go, internal/menubar/app_test.go</files>
  <read_first>internal/menubar/runtime_darwin.go, internal/menubar/app.go, internal/menubar/app_test.go, .planning/phases/04-menu-bar-and-dashboard-ux/04-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: A runtime-level route-slot sync helper receives two `RouteItems` and exposes two visible menu entries with the exact hostnames and OpenURL values from `buildMenuState`.
    - Test 2: Clicking a visible route entry dispatches `dispatcher.openRoute()` with the exact daemon-provided `OpenURL` (`https://...` or `http://...`) per D-04, without recomputing scheme in UI code.
    - Test 3: Shrinking the route list hides stale route slots so previously visible items cannot open outdated URLs.
  </behavior>
  <action>Per D-01 and D-04, add failing tests before implementation. Create `internal/menubar/runtime_darwin_test.go` with runtime-focused coverage for route-slot synchronization and click dispatch. Extend `internal/menubar/app_test.go` only if needed to keep `RouteItems` hostname/OpenURL projection explicit. Do not add dispatcher-only tests for this gap; the verification failure is specifically in runtime wiring.</action>
  <acceptance_criteria>
    - `internal/menubar/runtime_darwin_test.go` exists and contains tests covering route-slot sync, click dispatch, and stale-slot hiding.
    - `internal/menubar/app_test.go` still asserts `RouteItems` preserve daemon-provided hostnames and `OpenURL` values.
    - The new runtime tests fail before Task 2 implementation because route items are not yet rendered/clickable.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/menubar -run 'TestMenubarBuildStateFromAdminData|TestRuntimeRoute' -count=1</automated>
  </verify>
  <done>RED coverage exists for the exact missing verification behavior: route entries rendered from `RouteItems`, exact `OpenURL` pass-through, and stale route entries hidden when the list shrinks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render dynamic route items and wire per-route clicks</name>
  <files>internal/menubar/runtime_darwin.go, internal/menubar/runtime_darwin_test.go</files>
  <read_first>internal/menubar/runtime_darwin.go, internal/menubar/app.go, internal/menubar/open.go, internal/menubar/runtime_darwin_test.go, .planning/phases/04-menu-bar-and-dashboard-ux/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Each active route appears as a selectable systray item using `routeMenuItem.Hostname` as the title.
    - Test 2: Selecting a route opens the exact `routeMenuItem.OpenURL` value from the daemon projection.
    - Test 3: When the active-route set changes, extra systray route slots become hidden and no longer trigger stale browser opens.
  </behavior>
  <action>Implement the missing runtime wiring in `internal/menubar/runtime_darwin.go`. Keep the runtime thin per D-01: source route entries only from `client.Routes()` → `buildMenuState(...).RouteItems`; do not inspect daemon internals or recompute URLs locally. Introduce a small testable route-slot manager/adapter inside the runtime layer that can: (1) create route menu items beneath the status line and above static actions, (2) grow the slot slice when more routes appear, (3) update each visible slot title and remembered `OpenURL`, (4) hide unused trailing slots when routes disappear, and (5) dispatch each visible slot click to `dispatcher.openRoute(context.Background(), route.OpenURL)` exactly as provided by the daemon per D-04. Preserve existing static actions (`Refresh Routes`, `Pause Routing`, `Open Dashboard`, `Open Logs`, `Run Doctor`, `Start at Login`) and existing pause/startup title behavior.</action>
  <acceptance_criteria>
    - `internal/menubar/runtime_darwin.go` contains runtime logic that consumes `state.RouteItems` and binds click handlers to `dispatcher.openRoute(...)`.
    - The runtime no longer ignores `RouteItems`; route menu entries are created, updated, and hidden based on the current route list.
    - `go test ./internal/menubar -count=1` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/menubar -count=1</automated>
  </verify>
  <done>The native menubar runtime exposes active routes as live menu entries and uses daemon-provided `OpenURL` values verbatim for route opening, closing the UI-03 blocker.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the native menubar and dashboard UX on macOS</name>
  <files>internal/menubar/runtime_darwin.go, internal/dashboard/templates/dashboard.html.tmpl</files>
  <read_first>.planning/phases/04-menu-bar-and-dashboard-ux/04-VERIFICATION.md, .planning/phases/04-menu-bar-and-dashboard-ux/04-UI-SPEC.md, cmd/devproxy/menubar.go</read_first>
  <action>After Tasks 1-2 pass, run the automated tests below, then perform the native macOS verification steps exactly as written. This checkpoint exists because the remaining proof requires real systray rendering and browser-launch behavior; do not replace it with additional unit tests. If either the menubar route list or dashboard degraded copy looks wrong, stop and report the concrete mismatch instead of editing unrelated UI surfaces.</action>
  <what-built>Dynamic systray route entries now render from daemon route projections and route clicks open the daemon-provided `OpenURL`; dashboard degraded-state copy remains the approved UI contract.</what-built>
  <how-to-verify>
    1. Launch a devproxy daemon with at least one active managed route, then run `devproxy menubar` on macOS.
    2. Confirm the menu shows one item per active route above the static action group.
    3. Click a route that is HTTPS-ready and confirm the browser opens the exact HTTPS URL.
    4. If you have a route with degraded HTTPS readiness, click it and confirm the browser opens HTTP instead, matching the daemon-provided fallback behavior.
    5. Run `devproxy dashboard`, open `http://127.0.0.1:45831/`, and confirm health, routes, conflicts, and current-session errors remain legible and degraded copy only appears in true degraded conditions.
  </how-to-verify>
  <acceptance_criteria>
    - At least one active route is visible as a selectable menu item in the native macOS menubar.
    - Clicking a route opens the scheme chosen by the daemon (`https://` when ready, `http://` when degraded) without UI-side drift.
    - Dashboard sections remain visually clear and use the approved degraded copy only when the daemon is actually degraded/offline.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/menubar -count=1 && go test ./internal/dashboard -run TestDashboard -count=1 && go test ./cmd/devproxy -run TestDashboardCommandDefaultsKeepFixedLocalURLs -count=1</automated>
  </verify>
  <resume-signal>Type "approved" when the native menubar route-open flow and dashboard UX both look correct, or describe the issue you observed.</resume-signal>
  <done>Human verification confirms the blocker is closed in the real macOS systray/browser flow and no dashboard UX regression remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| menu bar runtime → admin API | Untrusted local UI events can only act through daemon-owned socket contracts. |
| menu bar runtime → browser opener | Route click data crosses into a shell-backed browser open call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | T | `internal/menubar/runtime_darwin.go` route click handling | mitigate | Use daemon-provided `RouteItems[].OpenURL` verbatim per D-04 and keep `open.go` http/https validation as the final guard before `open`. |
| T-04-05-02 | I | dynamic route slot cache | mitigate | Re-sync slot title/OpenURL pairs on every refresh and hide unused slots so stale routes cannot trigger outdated browser opens. |
| T-04-05-03 | E | startup and routing controls in systray | accept | This plan does not expand control scope; existing daemon-mediated admin API actions remain unchanged per D-01/D-02. |
</threat_model>

<verification>
- `go test ./internal/menubar -count=1`
- `go test ./internal/dashboard -run TestDashboard -count=1`
- `go test ./cmd/devproxy -run TestDashboardCommandDefaultsKeepFixedLocalURLs -count=1`
- Human macOS verification of native systray route opening and dashboard copy/state clarity.
</verification>

<success_criteria>
- UI-03 is satisfied in production runtime, not only in dispatcher tests.
- `internal/menubar/runtime_darwin.go` renders per-route menu entries and binds clicks to exact daemon-provided `OpenURL` values.
- Runtime tests cover route-slot creation, click dispatch, and stale-slot hiding.
- Human verification approves both the route-opening flow and the dashboard degraded-state UX.
</success_criteria>

<output>
After completion, create `.planning/phases/04-menu-bar-and-dashboard-ux/04-menu-bar-and-dashboard-ux-05-SUMMARY.md`
</output>
