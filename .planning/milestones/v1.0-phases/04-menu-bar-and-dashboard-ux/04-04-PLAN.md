---
phase: 04-menu-bar-and-dashboard-ux
plan: 04
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
files_modified:
  - internal/dashboard/server_test.go
  - internal/menubar/app_test.go
  - internal/dashboard/server.go
  - internal/menubar/app.go
  - cmd/devproxy/lifecycle_test.go
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
must_haves:
  truths:
    - "Menu bar and dashboard surface degraded/offline/startup-toggle failures clearly instead of silently failing."
    - "Open-dashboard, open-logs, refresh, and route-open flows still behave correctly when the daemon or HTTPS readiness is unhealthy."
    - "A human can verify the native menu bar and browser dashboard on macOS after automated checks pass."
  artifacts:
    - path: "internal/menubar/app_test.go"
      provides: "Regression coverage for menu fallback and launch/open failure states"
    - path: "internal/dashboard/server_test.go"
      provides: "Regression coverage for dashboard degraded/offline rendering and refresh feedback"
    - path: "cmd/devproxy/lifecycle_test.go"
      provides: "CLI-level integration coverage for dashboard/menubar launch paths"
  key_links:
    - from: "cmd/devproxy/lifecycle_test.go"
      to: "cmd/devproxy/dashboard.go"
      via: "dashboard launch and URL wiring"
      pattern: "dashboard|45831|logs"
    - from: "internal/menubar/app.go"
      to: "internal/dashboard/server.go"
      via: "open dashboard/open logs integration"
      pattern: "127\.0\.0\.1:45831|/logs"
---

<objective>
Harden the Phase 4 UI flows and finish with automated plus human verification.

Purpose: close the highest-risk UX gaps after the dashboard and menubar exist, especially failure visibility and end-to-end integration between the two UI surfaces.
Output: regression coverage for degraded flows, any required polish fixes, and a final macOS human-verification checkpoint.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-CONTEXT.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-RESEARCH.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-UI-SPEC.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-02-PLAN.md
@.planning/phases/04-menu-bar-and-dashboard-ux/04-03-PLAN.md
@internal/dashboard/server.go
@internal/menubar/app.go

<interfaces>
Expected fixed URLs from prior plans:
```text
Dashboard: http://127.0.0.1:45831/
Logs:      http://127.0.0.1:45831/logs
```

Expected failure-sensitive fields from prior plans:
```text
RouteView.OpenURL
RouteView.FallbackReason
Startup role status_message / toggleable
Session issue list (bounded, newest-first)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression coverage for degraded UI flows and repair any failures</name>
  <files>internal/dashboard/server_test.go, internal/menubar/app_test.go, cmd/devproxy/lifecycle_test.go, internal/dashboard/server.go, internal/menubar/app.go</files>
  <read_first>internal/dashboard/server.go, internal/menubar/app.go, internal/dashboard/server_test.go, internal/menubar/app_test.go, cmd/devproxy/lifecycle_test.go</read_first>
  <behavior>
    - Test 1: admin API unreachable state renders the approved dashboard error copy and a degraded menubar state without removing repair actions.
    - Test 2: start-at-login toggle failures are reflected in session issues/menu text and do not leave optimistic success UI behind.
    - Test 3: routes with unhealthy HTTPS readiness still open HTTP and show the fallback reason in UI-visible surfaces.
    - Test 4: dashboard/log launch paths keep the fixed localhost URLs and do not drift from plan 02/03 contracts.
  </behavior>
  <action>Write regression tests first, then patch any failing menubar/dashboard code to satisfy them. Use `cmd/devproxy/lifecycle_test.go` for CLI-level launch wiring checks, `internal/dashboard/server_test.go` for browser-facing degraded/offline rendering, and `internal/menubar/app_test.go` for menu fallback state and start-at-login failure handling. Keep fixes tightly scoped to the surfaced failures; do not expand into new dashboard features or persisted preferences.</action>
  <verify>
    <automated>go test ./internal/dashboard/... ./internal/menubar/... ./cmd/devproxy/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>Regression tests explicitly cover admin-offline copy, startup-toggle failure text, HTTP fallback when HTTPS is unhealthy, and fixed dashboard/log URLs; any production-code changes are limited to satisfying those assertions.</acceptance_criteria>
  <done>Phase 4 has automated coverage for the highest-risk degraded paths and any discovered UI regressions are fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify native menu bar and browser dashboard behavior on macOS</name>
  <files>None — checkpoint only</files>
  <action>After Task 1 passes, run the native macOS verification flow against the built dashboard and menubar binaries. Use this checkpoint only to confirm the automated work behaves correctly in the real menu bar and browser environment; do not add new scope unless the verification reveals a concrete bug.</action>
  <what-built>Dashboard and menubar flows backed by the daemon admin API, including route opening, refresh, logs, pause/resume, and start-at-login feedback.</what-built>
  <how-to-verify>
    1. Start the daemon and at least one routed Compose project.
    2. Run the automated checks from this plan, then launch `devproxy dashboard` and confirm the page shows daemon health, route rows, recent conflicts, and recent current-session errors.
    3. Launch `devproxy menubar` on macOS and confirm the menu shows daemon state first, then active routes, then operator actions.
    4. Trigger Refresh Routes, Open Dashboard, Open Logs, Pause/Resume Routing, and toggle the menubar start-at-login control; confirm each action either succeeds visibly or shows an explicit failure message.
    5. Open a route whose HTTPS path is healthy and confirm it opens `https://...`; then force an HTTPS-unhealthy state and confirm the route opens `http://...` with fallback messaging preserved in the UI.
  </how-to-verify>
  <verify>
    <automated>go test ./internal/dashboard/... ./internal/menubar/... ./cmd/devproxy/... && go test ./...</automated>
  </verify>
  <done>Human verification is recorded as approved, or the exact remaining UI defects are captured for follow-up.</done>
  <resume-signal>Type "approved" or describe the remaining UI issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated tests -> live launch paths | Integration coverage exercises real localhost URLs and UI launch wiring |
| human verification -> native macOS surfaces | Final trust check happens outside unit-test mocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | R | degraded UI flows | mitigate | Preserve explicit session issue/error text in both dashboard and menubar so failures remain auditable after manual verification. |
| T-04-14 | I | route-open fallback UX | mitigate | Test and manually verify that HTTP fallback messaging is visible when HTTPS readiness is false. |
| T-04-15 | D | dashboard/menubar launch loops | mitigate | Keep fixed localhost launch targets and regression-test that repair actions remain available when daemon/API connectivity fails. |
</threat_model>

<verification>
- `go test ./internal/dashboard/... ./internal/menubar/... ./cmd/devproxy/...`
- `go test ./...`
- Human verification checkpoint approved on macOS
</verification>

<success_criteria>
- Automated regression checks cover degraded/offline/startup-toggle failure states.
- Human verification confirms the native menubar and browser dashboard match the planned Phase 4 behavior on macOS.
</success_criteria>

<output>
After completion, create `.planning/phases/04-menu-bar-and-dashboard-ux/04-menu-bar-and-dashboard-ux-04-SUMMARY.md`
</output>
