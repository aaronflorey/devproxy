---
phase: 03-install-daemon-lifecycle-and-diagnostics
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - cmd/devproxy/status.go
  - cmd/devproxy/routes.go
  - cmd/devproxy/refresh.go
  - cmd/devproxy/logs.go
  - internal/adminapi/client.go
  - internal/adminapi/client_test.go
autonomous: true
requirements:
  - OPS-04
  - OPS-05
  - OPS-07
  - OPS-09
must_haves:
  truths:
    - "`status`, `routes`, `refresh`, and `logs` talk to the live daemon over the local admin socket instead of rebuilding route or health state."
    - "`status` shows daemon health, install/runtime state, and active route counts from the same control plane source."
    - "`logs` returns current-session daemon events only, matching the daemon-owned session model and deferred-history scope."
  artifacts:
    - path: "internal/adminapi/client.go"
      provides: "thin admin socket client for CLI commands"
    - path: "cmd/devproxy/status.go"
      provides: "status command rendering daemon health from the admin API"
    - path: "cmd/devproxy/logs.go"
      provides: "current-session log streaming/printing over the shared admin API"
  key_links:
    - from: "cmd/devproxy/status.go"
      to: "internal/adminapi/client.go"
      via: "socket-backed request for status payload"
      pattern: "Status"
    - from: "cmd/devproxy/refresh.go"
      to: "internal/adminapi/server.go"
      via: "POST refresh control action"
      pattern: "refresh"
---

<objective>
Build the thin operator commands that consume the Phase 3 control plane.

Purpose: satisfy D-01 by making inspection and refresh surfaces read or trigger live daemon behavior through one admin API source.
Output: admin socket client plus `status`, `routes`, `refresh`, and `logs` commands.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define socket client behavior for operator commands</name>
  <files>internal/adminapi/client.go, internal/adminapi/client_test.go</files>
  <read_first>internal/adminapi/types.go, internal/adminapi/server.go, internal/admin/status.go, internal/admin/routes.go, internal/admin/logs.go</read_first>
  <behavior>
    - Test 1: per D-01, status/routes/logs requests decode the daemon JSON payloads from the UNIX socket without local recomputation.
    - Test 2: refresh sends one control-plane action to the daemon and surfaces daemon-side errors verbatim.
    - Test 3: missing socket or malformed responses return explicit operator-facing errors instead of silent empty output.
  </behavior>
  <action>Create a small admin API client and tests for GET/POST operations over the daemon socket, covering D-01 thin-client behavior and current-session log retrieval for OPS-07. Keep the client focused on request execution, JSON decoding, and actionable local-socket errors; do not add business logic or fallback state paths.</action>
  <verify>
    <automated>go test ./internal/adminapi/... -run 'TestClient'</automated>
  </verify>
  <acceptance_criteria>Client tests cover status, routes, refresh, logs, and explicit no-socket / bad-response failures.</acceptance_criteria>
  <done>Later CLI commands can rely on one tested client instead of duplicating socket logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement status, routes, refresh, and logs as thin clients</name>
  <files>cmd/devproxy/status.go, cmd/devproxy/routes.go, cmd/devproxy/refresh.go, cmd/devproxy/logs.go</files>
  <read_first>internal/adminapi/client_test.go, cmd/devproxy/commands.go, internal/admin/status.go, internal/admin/routes.go, internal/admin/logs.go, .planning/REQUIREMENTS.md</read_first>
  <action>Create four Cobra commands that register through the command-registry helper from Plan 01 and consume `internal/adminapi/client.go` only, per D-01. `status` must report daemon health, install/runtime state, and route counts for OPS-04; `routes` must list active mappings and conflict losers for OPS-05; `refresh` must trigger a full container rescan through the daemon API; `logs` must print current-session daemon events only and must not promise persisted history because that idea is deferred.</action>
  <verify>
    <automated>go test ./cmd/devproxy/... ./internal/adminapi/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>All four commands compile, register without editing `root.go`, and depend on the admin socket client rather than direct daemon, registry, or Docker access.</acceptance_criteria>
  <done>Core inspection commands now behave as control-plane clients instead of alternate state engines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal user input → CLI thin clients | Local operator input becomes admin API requests and formatted output. |
| CLI thin client → admin UNIX socket | Commands depend on daemon-provided state and must surface transport errors safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | R | cmd/devproxy/status.go | mitigate | Render only daemon-provided status payloads through the shared client so status truth matches the control plane per D-01. |
| T-03-05 | D | internal/adminapi/client.go | mitigate | Return explicit socket/malformed-response errors so missing daemon state fails loudly instead of implying healthy empty output. |
| T-03-06 | I | cmd/devproxy/logs.go | accept | Current-session logs are intentionally local operator-visible diagnostics; auth is out of v1 scope because the socket remains local-only. |
</threat_model>

<verification>
- `go test ./internal/adminapi/...`
- `go test ./cmd/devproxy/...`
- `go test ./...`
</verification>

<success_criteria>
- `status`, `routes`, `refresh`, and `logs` all consume one daemon-owned admin API.
- Operator inspection no longer depends on local state recomputation paths.
</success_criteria>

<output>
After completion, create `.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-install-daemon-lifecycle-and-diagnostics-02-SUMMARY.md`
</output>
