---
phase: 03-install-daemon-lifecycle-and-diagnostics
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/devproxy/install.go
  - cmd/devproxy/uninstall.go
  - cmd/devproxy/lifecycle_test.go
  - internal/install/install.go
  - internal/install/install_test.go
  - internal/install/uninstall.go
  - internal/install/uninstall_test.go
  - internal/install/launchd.go
autonomous: true
gap_closure: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-08
must_haves:
  truths:
    - "Per D-04, D-06, and D-08, `devproxy install` fails before partial writes with an explicit rerun-with-sudo message when the process cannot manage system launchd paths, resolver files, and install directories."
    - "Per D-05 and D-08, `devproxy uninstall` fails before destructive work with an explicit rerun-with-sudo message when the process cannot stop/remove system launchd services or resolver state."
    - "Per D-05, uninstall teardown is idempotent for missing launchd service state so selected cleanup can still proceed after already-removed services/resolver artifacts."
  artifacts:
    - path: "cmd/devproxy/lifecycle_test.go"
      provides: "CLI regression coverage for lifecycle privilege preflights"
    - path: "internal/install/install.go"
      provides: "install privilege preflight before path/resolver/service mutation"
    - path: "internal/install/uninstall.go"
      provides: "safe uninstall teardown with privilege guidance and idempotent service stop behavior"
  key_links:
    - from: "cmd/devproxy/install.go"
      to: "internal/install/install.go"
      via: "privilege preflight before installer invocation"
      pattern: "requires root privileges|sudo"
    - from: "internal/install/uninstall.go"
      to: "internal/install/launchd.go"
      via: "bootout/uninstall helpers tolerant of already-removed service state"
      pattern: "Could not find service|Boot-out failed"
---

<objective>
Close the real-macOS install/uninstall lifecycle gaps surfaced by 03-HUMAN-UAT.

Purpose: make Phase 3 lifecycle commands fail clearly when root access is required and avoid false destructive failures when launchd service state is already gone.
Output: privilege-aware install/uninstall command flow plus idempotent launchd teardown coverage.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-HUMAN-UAT.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-install-daemon-lifecycle-and-diagnostics-03-SUMMARY.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-install-daemon-lifecycle-and-diagnostics-04-SUMMARY.md

<interfaces>
From internal/install/install.go:
```go
type Options struct {
	Suffix      string
	WithMenubar bool
	Paths       InstallPaths
}

func (i *Installer) Install(ctx context.Context, opts Options) error
```

From internal/install/uninstall.go:
```go
type CleanupScope struct {
	Config       bool
	State        bool
	Logs         bool
	Certificates bool
}

type UninstallOptions struct {
	Suffix      string
	WithMenubar bool
	Paths       InstallPaths
	Cleanup     CleanupScope
}

func (u *Uninstaller) Uninstall(ctx context.Context, opts UninstallOptions) error
```

From internal/install/launchd.go:
```go
type LaunchdServiceConfig struct {
	Label     string
	Domain    LaunchdDomain
	PlistPath string
	Program   string
	Arguments []string
}

func StopService(ctx context.Context, cfg LaunchdServiceConfig) error
func UninstallService(ctx context.Context, cfg LaunchdServiceConfig) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock lifecycle privilege and idempotent teardown regressions in tests</name>
  <files>cmd/devproxy/lifecycle_test.go, internal/install/install_test.go, internal/install/uninstall_test.go</files>
  <read_first>cmd/devproxy/install.go, cmd/devproxy/uninstall.go, internal/install/install.go, internal/install/uninstall.go, internal/install/launchd.go, .planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-HUMAN-UAT.md</read_first>
  <behavior>
    - Test 1: per D-04, D-06, and D-08, `devproxy install` returns an explicit root-required error before `EnsurePaths`, `WriteResolver`, or launchd registration runs when EUID is not 0.
    - Test 2: per D-05 and D-08, `devproxy uninstall` returns an explicit root-required error before cleanup prompts or launchctl teardown when EUID is not 0.
    - Test 3: per D-05, uninstall treats already-missing daemon/menubar launchd state as non-fatal and still reaches resolver removal plus the selected cleanup scope.
  </behavior>
  <action>Add regression tests for the UAT failures in 03-HUMAN-UAT.md. Pin the exact actionable guidance strings `devproxy install requires root privileges; rerun with sudo` and `devproxy uninstall requires root privileges; rerun with sudo`, and assert they happen before any filesystem or launchctl side effect per D-04, D-05, D-06, and D-08. Extend uninstall tests so `Could not find service`, missing plist files, and already-removed bootout state are treated as idempotent teardown instead of fatal blockers.</action>
  <verify>
    <automated>go test ./cmd/devproxy/... ./internal/install/... -run 'Test(Install|Uninstall|Lifecycle)'</automated>
  </verify>
  <acceptance_criteria>`cmd/devproxy/lifecycle_test.go` exists, the new tests assert the exact `requires root privileges` guidance, and uninstall tests cover already-missing launchd state plus continued scoped cleanup.</acceptance_criteria>
  <done>Privilege and teardown behavior is fixed in tests before lifecycle code changes begin.</done>
</task>

<task type="auto">
  <name>Task 2: Implement root-aware lifecycle preflights and idempotent launchd teardown</name>
  <files>cmd/devproxy/install.go, cmd/devproxy/uninstall.go, internal/install/install.go, internal/install/install_test.go, internal/install/uninstall.go, internal/install/uninstall_test.go, internal/install/launchd.go</files>
  <read_first>cmd/devproxy/lifecycle_test.go, internal/install/install_test.go, internal/install/uninstall_test.go, internal/install/paths.go, internal/install/resolver.go</read_first>
  <action>Implement explicit privilege preflights for install and uninstall so Phase 3 lifecycle work stays orchestration-only per D-04 and never starts partial writes when root privileges are missing. `devproxy install` must fail before `EnsurePaths` with actionable sudo guidance that names the privileged targets `/usr/local/etc/devproxy`, `/var/lib/devproxy`, `/var/log/devproxy`, `/etc/resolver`, and `/Library/LaunchDaemons`. `devproxy uninstall` must fail before prompting when it cannot manage the system daemon domain or resolver state per D-05 and D-08. In `internal/install/launchd.go`, make bootout/uninstall idempotent by treating missing plist files and launchctl output containing `Could not find service` or equivalent already-removed state as success, while preserving hard failures for real permission or syntax errors.</action>
  <verify>
    <automated>go test ./cmd/devproxy/... ./internal/install/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`cmd/devproxy/install.go` and `cmd/devproxy/uninstall.go` surface exact sudo guidance before side effects, `internal/install/launchd.go` tolerates already-removed service state, and targeted plus full Go tests pass.</acceptance_criteria>
  <done>Real-macOS lifecycle commands now fail clearly on missing privileges and uninstall no longer dies on already-removed launchd state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI lifecycle commands → privileged macOS files/services | Local operator input drives writes to system directories and launchd domains. |
| launchctl / filesystem state → uninstall cleanup flow | External OS state can be partially missing or already removed before teardown runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | E | cmd/devproxy/install.go | mitigate | Require explicit root preflight before touching `/usr/local`, `/var`, `/etc/resolver`, or `/Library/LaunchDaemons` so privileged operations fail loudly and early. |
| T-03-14 | D | internal/install/uninstall.go | mitigate | Make uninstall teardown idempotent for missing launchd state so cleanup can proceed without false fatal stops while keeping real permission failures visible. |
| T-03-15 | T | internal/install/launchd.go | mitigate | Match only known already-removed launchctl states as non-fatal and preserve other bootout/install errors verbatim to avoid masking real service problems. |
</threat_model>

<verification>
- `go test ./cmd/devproxy/... ./internal/install/... -run 'Test(Install|Uninstall|Lifecycle)'`
- `go test ./cmd/devproxy/... ./internal/install/...`
- `go test ./...`
</verification>

<success_criteria>
- `devproxy install` tells the operator exactly when sudo is required before any partial install writes happen.
- `devproxy uninstall` tells the operator exactly when sudo is required and treats already-removed launchd state as safe teardown, not a blocker.
</success_criteria>

<output>
After completion, create `.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-install-daemon-lifecycle-and-diagnostics-05-SUMMARY.md`
</output>
