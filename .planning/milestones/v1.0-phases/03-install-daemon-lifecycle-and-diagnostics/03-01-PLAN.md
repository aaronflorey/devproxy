---
phase: 03-install-daemon-lifecycle-and-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/devproxy/root.go
  - cmd/devproxy/commands.go
  - cmd/devproxy/daemon.go
  - internal/adminapi/types.go
  - internal/adminapi/server.go
  - internal/adminapi/server_test.go
  - internal/daemon/app.go
  - internal/daemon/app_test.go
autonomous: true
requirements:
  - OPS-03
  - OPS-09
must_haves:
  truths:
    - "Per D-02 and D-03, `devproxy daemon` exits immediately with explicit errors when Docker, mkcert prerequisites, socket ownership, or required listener binds are unavailable, and it extends existing network/cert failure signaling instead of falling back silently."
    - "Per D-09, the daemon exposes one local admin API UNIX socket with explicit local-only ownership/mode controls and stale-socket cleanup that later CLI surfaces can consume for status, routes, refresh, doctor, and logs."
    - "Daemon-owned health and snapshot state are served without recomputing operator views inside individual commands."
  artifacts:
    - path: "internal/adminapi/server.go"
      provides: "UNIX-socket HTTP+JSON admin server with stale-socket cleanup and local-only lifecycle"
    - path: "internal/daemon/app.go"
      provides: "foreground daemon bootstrap, validation, and refresh orchestration"
    - path: "cmd/devproxy/daemon.go"
      provides: "CLI entrypoint for the fail-fast foreground daemon"
  key_links:
    - from: "internal/adminapi/server.go"
      to: "internal/admin/status.go"
      via: "JSON handlers publishing shared read models"
      pattern: "BuildStatus|RoutesFromSnapshot|BuildDoctor|BuildSessionEvents"
    - from: "internal/daemon/app.go"
      to: "internal/daemon/network.go"
      via: "startup validation and runtime assembly"
      pattern: "NewNetworkRuntime|Start"
---

<objective>
Create the daemon-owned control plane and foreground daemon entrypoint for Phase 3.

Purpose: establish one authoritative local admin surface and fail-fast daemon startup path before any operator or install commands build on it.
Output: admin API contracts/server, daemon app bootstrap, and `devproxy daemon` command wiring.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-RESEARCH.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-local-dns-proxy-and-https-serving-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the admin socket and daemon bootstrap contracts</name>
  <files>internal/adminapi/types.go, internal/adminapi/server_test.go, internal/daemon/app_test.go</files>
  <read_first>internal/admin/status.go, internal/admin/routes.go, internal/admin/doctor.go, internal/admin/logs.go, internal/daemon/network.go, internal/daemon/events.go, internal/certs/mkcert.go</read_first>
  <behavior>
    - Test 1: per D-09, stale socket files are removed before bind and the new socket is created with explicit local-only mode/ownership expectations.
    - Test 2: per D-02, daemon bootstrap fails clearly when Docker ping fails, mkcert prerequisites fail, or HTTP/HTTPS listener binds fail.
    - Test 3: per D-01, admin handlers serialize shared status/routes/doctor/log payloads from one daemon state source without alternate recomputation paths.
  </behavior>
  <action>Create admin API request/response contract types and tests for a UNIX-socket HTTP+JSON server plus daemon bootstrap orchestration. The tests must pin D-09 socket cleanup and permission behavior, D-02 startup failure semantics, and D-01 shared read-model publication before implementation. Include a small command-registry contract so later plans can add Cobra subcommands without touching `root.go` again.</action>
  <verify>
    <automated>go test ./internal/adminapi/... ./internal/daemon/... -run 'Test(AdminAPI|DaemonApp)'</automated>
  </verify>
  <acceptance_criteria>Failing tests describe socket cleanup, JSON contracts, and explicit bootstrap failures; command registration scaffolding expectations are defined for later status/install/doctor plans.</acceptance_criteria>
  <done>Control-plane and daemon lifecycle behavior is fixed in tests before production wiring begins.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the daemon app, socket server, and foreground command</name>
  <files>cmd/devproxy/root.go, cmd/devproxy/commands.go, cmd/devproxy/daemon.go, internal/adminapi/types.go, internal/adminapi/server.go, internal/daemon/app.go</files>
  <read_first>internal/adminapi/server_test.go, internal/daemon/app_test.go, cmd/devproxy/root.go, internal/daemon/reconciler.go, internal/daemon/network.go, internal/certs/mkcert.go</read_first>
  <action>Implement a daemon app that owns the reconciler, watcher health, network runtime, and admin projections, then exposes them through a local UNIX-socket HTTP+JSON server per D-01 and D-09. `devproxy daemon` must validate Docker reachability, mkcert prerequisites, and listener binds per D-02 and D-03, then start the admin socket and runtime without daemonizing. Update `cmd/devproxy/root.go` once to use a command-registration helper from `cmd/devproxy/commands.go`, registering `print-config` and the new daemon command so later plans can add more commands without another `root.go` edit.</action>
  <verify>
    <automated>go test ./internal/adminapi/... ./internal/daemon/... ./cmd/devproxy/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`internal/adminapi/server.go` serves JSON views over a UNIX socket, `internal/daemon/app.go` owns startup validation and refresh state, and `devproxy daemon` compiles as the foreground daemon path with fail-loud startup behavior.</acceptance_criteria>
  <done>The shared control plane exists and later plans can treat it as the only source of daemon state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI process → admin UNIX socket | Untrusted local client input crosses into daemon-owned control APIs. |
| Docker/mkcert/runtime prerequisites → daemon bootstrap | External system dependencies determine whether privileged local services can start safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | internal/adminapi/server.go | mitigate | Bind only to a daemon-owned UNIX socket with explicit file mode and remove stale socket files before listen per D-09. |
| T-03-02 | D | internal/daemon/app.go | mitigate | Validate Docker, mkcert, and listener binds before serving so the daemon exits loudly instead of entering a partial-running state per D-02/D-03. |
| T-03-03 | R | internal/adminapi/types.go | mitigate | Serve status/routes/doctor/logs from one daemon-owned state graph so operator commands cannot drift from the authoritative runtime per D-01. |
</threat_model>

<verification>
- `go test ./internal/adminapi/... ./internal/daemon/... ./cmd/devproxy/...`
- `go test ./...`
</verification>

<success_criteria>
- A single daemon-owned UNIX socket control plane exists for all later operator surfaces.
- Foreground daemon startup fails clearly when required local dependencies are unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-install-daemon-lifecycle-and-diagnostics-01-SUMMARY.md`
</output>
