---
phase: 03-install-daemon-lifecycle-and-diagnostics
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 03
files_modified:
  - cmd/devproxy/doctor.go
  - cmd/devproxy/uninstall.go
  - internal/doctor/checks.go
  - internal/doctor/checks_test.go
  - internal/install/uninstall.go
  - internal/install/uninstall_test.go
autonomous: true
requirements:
  - OPS-06
  - OPS-08
must_haves:
  truths:
    - "`devproxy doctor` checks Docker reachability, resolver activation, port binding, mkcert/local CA state, launchd state, admin/proxy reachability, and example managed-domain resolution."
    - "Doctor validates macOS resolver state using `scutil --dns`-aligned evidence instead of only checking raw files or DNS tools."
    - "`devproxy uninstall` prompts for retention or removal of config, state, logs, and certificates, then removes only the selected scope."
  artifacts:
    - path: "internal/doctor/checks.go"
      provides: "diagnostic checks for runtime, resolver, launchd, and hostname reachability"
    - path: "internal/install/uninstall.go"
      provides: "selective uninstall orchestration for config/state/log/cert cleanup"
    - path: "cmd/devproxy/uninstall.go"
      provides: "CLI uninstall prompt flow"
  key_links:
    - from: "internal/doctor/checks.go"
      to: "scutil --dns"
      via: "resolver-state validation per D-10"
      pattern: "scutil"
    - from: "cmd/devproxy/uninstall.go"
      to: "internal/install/uninstall.go"
      via: "prompted cleanup scope selection per D-05"
      pattern: "retain|remove"
---

<objective>
Finish Phase 3 with full diagnostics and safe uninstall flows.

Purpose: close the operator lifecycle by verifying real macOS runtime state and removing only the artifacts the developer chooses to clean up.
Output: doctor checks, uninstall orchestration, and their CLI commands.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-CONTEXT.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-RESEARCH.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-01-PLAN.md
@.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define doctor and uninstall behaviors before implementation</name>
  <files>internal/doctor/checks_test.go, internal/install/uninstall_test.go</files>
  <read_first>internal/adminapi/types.go, internal/install/install.go, .planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: per D-10 and OPS-06, doctor verifies Docker reachability, launchd state, admin socket reachability, listener bind health, resolver activation from `scutil --dns`-aligned data, and example managed-domain resolution.
    - Test 2: per D-05 and OPS-08, uninstall honors independent keep/remove choices for config, state, logs, and certificates.
    - Test 3: uninstall always unregisters running services and resolver wiring safely before optional artifact deletion.
  </behavior>
  <action>Create tests for doctor and uninstall behavior first. The doctor tests must lock D-10 system-resolver validation and live-control-plane checks rather than file-only heuristics. The uninstall tests must lock D-05 selective cleanup semantics so no category is removed without an explicit user choice.</action>
  <verify>
    <automated>go test ./internal/doctor/... ./internal/install/... -run 'Test(Doctor|Uninstall)'</automated>
  </verify>
  <acceptance_criteria>Tests exist for resolver-state validation, admin/proxy reachability, launchd checks, and selective uninstall cleanup scope.</acceptance_criteria>
  <done>Diagnostics and uninstall safety rules are fixed before CLI implementation begins.</done>
</task>

<task type="auto">
  <name>Task 2: Implement doctor checks, uninstall orchestration, and CLI prompts</name>
  <files>cmd/devproxy/doctor.go, cmd/devproxy/uninstall.go, internal/doctor/checks.go, internal/install/uninstall.go</files>
  <read_first>internal/doctor/checks_test.go, internal/install/uninstall_test.go, cmd/devproxy/commands.go, internal/adminapi/client.go, internal/install/launchd.go</read_first>
  <action>Implement `devproxy doctor` to gather Docker, launchd, admin socket, listener, mkcert/local CA, resolver, and example-hostname checks, using `scutil --dns`-aligned resolver evidence per D-10 and the daemon control plane where appropriate. Implement `devproxy uninstall` and its helper orchestration so services are stopped/unregistered, resolver wiring is removed, and config/state/log/cert deletion follows the user-selected retention choices per D-05. Keep persisted multi-session log cleanup out of scope because that capability is deferred.</action>
  <verify>
    <automated>go test ./internal/doctor/... ./internal/install/... ./cmd/devproxy/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`doctor` and `uninstall` commands register through the command registry, doctor checks use system-resolver evidence plus live daemon state, and uninstall only removes categories explicitly chosen by the developer.</acceptance_criteria>
  <done>Phase 3 ends with reliable troubleshooting coverage and safe uninstall behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local system state → doctor output | Diagnostics interpret OS and daemon state into operator decisions. |
| User uninstall choices → destructive local cleanup | Prompted selections control removal of local files and registered services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | R | internal/doctor/checks.go | mitigate | Check resolver activation through `scutil --dns`-aligned data plus live daemon/admin reachability so doctor output matches actual macOS runtime state per D-10. |
| T-03-11 | D | internal/install/uninstall.go | mitigate | Stop and unregister services before cleanup so uninstall does not leave active listeners or resolver state behind. |
| T-03-12 | T | cmd/devproxy/uninstall.go | mitigate | Require explicit keep/remove choices for config, state, logs, and certificates so cleanup scope cannot silently exceed the user's decision per D-05. |
</threat_model>

<verification>
- `go test ./internal/doctor/... ./internal/install/... ./cmd/devproxy/...`
- `go test ./...`
</verification>

<success_criteria>
- `devproxy doctor` verifies real system and daemon state, including resolver activation.
- `devproxy uninstall` removes only the artifacts the developer chose to delete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-install-daemon-lifecycle-and-diagnostics/03-install-daemon-lifecycle-and-diagnostics-04-SUMMARY.md`
</output>
