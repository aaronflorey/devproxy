---
phase: 02-local-dns-proxy-and-https-serving
plan: 02
type: tdd
wave: 2
depends_on:
  - 01
files_modified:
  - internal/dns/server.go
  - internal/dns/server_test.go
  - internal/admin/status.go
autonomous: true
requirements:
  - NET-01
  - NET-07
  - NET-08
must_haves:
  truths:
    - "The local DNS server answers only for hostnames under the configured managed suffix and always returns 127.0.0.1 for those matches."
    - "Managed-host detection logic is reusable by HTTP and HTTPS listeners for no-route and paused responses."
    - "DNS remains active while routing is paused."
  artifacts:
    - path: "internal/dns/server.go"
      provides: "authoritative managed-suffix DNS responder and hostname classification helpers"
    - path: "internal/admin/status.go"
      provides: "listener-facing DNS health/status projection hooks"
  key_links:
    - from: "internal/dns/server.go"
      to: "internal/routing/types.go"
      via: "managed-host lookup against snapshot routes"
      pattern: "Snapshot"
---

<objective>
Build the managed-suffix DNS responder and hostname classification logic that makes local domains resolve predictably.

Purpose: satisfy wildcard resolver behavior early and give the proxy listeners a shared managed-host check for no-route and paused responses.
Output: tested DNS server package and reusable managed-host lookup helpers.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-CONTEXT.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-RESEARCH.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing DNS tests for managed suffix behavior</name>
  <files>internal/dns/server_test.go</files>
  <read_first>internal/routing/types.go, .planning/REQUIREMENTS.md, .planning/phases/02-local-dns-proxy-and-https-serving/02-CONTEXT.md</read_first>
  <action>Create tests that cover these exact cases: a hostname under the configured managed suffix returns an A record for `127.0.0.1`; a hostname outside the suffix receives no authoritative local answer; an active route under the suffix is recognized as managed; a missing route under the suffix is still classified as managed for friendly no-route handling; and paused routing does not change the DNS answer path for managed suffix hostnames.</action>
  <verify>
    <automated>go test ./internal/dns/... -run 'Test(ManagedSuffix|HostnameLookup)'</automated>
  </verify>
  <acceptance_criteria>DNS tests include positive and negative suffix cases, plus a managed-no-route case and a paused-routing case that still resolves to 127.0.0.1.</acceptance_criteria>
  <done>RED tests define the DNS behavior and managed-host classification required by NET-01, NET-07, and NET-08.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the DNS responder and managed-host helpers</name>
  <files>internal/dns/server.go, internal/admin/status.go</files>
  <read_first>internal/dns/server_test.go, internal/admin/status.go, internal/registry/snapshot.go, internal/routing/types.go</read_first>
  <action>Create `internal/dns/server.go` using `github.com/miekg/dns` with a server type that accepts the managed suffix and a snapshot reader, answers A-record requests for matching suffix hostnames with `127.0.0.1`, and exposes helper methods that classify whether a Host belongs to the managed suffix and whether it currently has an active winning route. Update `internal/admin/status.go` so future status surfaces can report DNS listener health and the configured managed suffix without recomputing routing state.</action>
  <verify>
    <automated>go test ./internal/dns/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`internal/dns/server.go` exists and uses `github.com/miekg/dns`; tests prove only the configured suffix resolves locally; admin status structures expose DNS listener health metadata for later CLI use.</acceptance_criteria>
  <done>Managed local domains resolve correctly and later HTTP/HTTPS listeners can share the same hostname classification logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hostname query → local DNS responder | Arbitrary local hostname lookups hit the daemon's managed-suffix resolver. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | S | internal/dns/server.go | mitigate | Restrict authoritative answers to the configured managed suffix so unrelated hostnames are not spoofed locally. |
| T-02-05 | D | internal/dns/server.go | mitigate | Keep DNS answers independent from paused-routing state so pause does not accidentally break name resolution. |
</threat_model>

<verification>
- `go test ./internal/dns/...`
- `go test ./...`
</verification>

<success_criteria>
- The DNS server answers only for the managed suffix.
- Managed-host classification is reusable by later no-route and paused HTTP/HTTPS responses.
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-dns-proxy-and-https-serving/02-local-dns-proxy-and-https-serving-02-SUMMARY.md`
</output>
