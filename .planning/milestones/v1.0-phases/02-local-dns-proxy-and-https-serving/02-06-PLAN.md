---
phase: 02-local-dns-proxy-and-https-serving
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/daemon/reconciler.go
  - internal/daemon/reconciler_test.go
autonomous: true
gap_closure: true
requirements:
  - NET-02
  - NET-06
must_haves:
  truths:
    - Override-driven hostnames are published in the routing snapshot before any request reaches the proxy.
    - Label-over-config precedence still decides the upstream port and scheme used for HTTP and WebSocket proxying.
  artifacts:
    - path: internal/daemon/reconciler.go
      provides: merged override aware route construction
      contains: routing.MergeOverrides(
    - path: internal/daemon/reconciler_test.go
      provides: regression coverage for config override and label precedence routing metadata
      contains: TestReconcilerAppliesMergedOverrides
  key_links:
    - from: internal/daemon/reconciler.go
      to: internal/routing/overrides.go
      via: routing.MergeOverrides
      pattern: routing\.MergeOverrides\(
    - from: internal/daemon/reconciler.go
      to: internal/discovery/ports.go
      via: SelectPublishedPort with config override port
      pattern: SelectPublishedPort\(c\.Ports, routeOpts, configPort\)
---

<objective>
Restore override-aware route publishing so Phase 2 listeners proxy the hostname, port, and scheme the developer actually configured.

Purpose: Close the NET-02 / NET-06 gap caused by `internal/daemon/reconciler.go` ignoring merged config+label preferences during snapshot publication.
Output: Reconciler tests and implementation that merge config overrides with labels before domain generation and upstream target selection.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-CONTEXT.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-RESEARCH.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-VERIFICATION.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-local-dns-proxy-and-https-serving-01-SUMMARY.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-local-dns-proxy-and-https-serving-03-SUMMARY.md

<interfaces>
From `internal/routing/overrides.go`:
```go
type RoutePreferences struct {
    Enable   *bool
    Domain   string
    Domains  []string
    Root     *bool
    Port     *int
    Scheme   string
    Priority *int
}

func MergeOverrides(config, labels RoutePreferences) (RoutePreferences, []Warning)
```

From `internal/discovery/ports.go`:
```go
func SelectPublishedPort(ports []PublishedPort, opts RouteOptions, configOverridePort int) (PublishedPort, string, bool)
```

From `internal/routing/domains.go`:
```go
func GenerateDomains(project, service string, prefs RoutePreferences, opts RouteOptions) ([]string, []Warning)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock merged-override reconciler behavior with failing tests</name>
  <read_first>
    internal/daemon/reconciler_test.go
    internal/daemon/reconciler.go
    internal/routing/overrides.go
    internal/routing/domains.go
    internal/discovery/metadata.go
    internal/discovery/ports.go
    internal/config/config.go
  </read_first>
  <files>internal/daemon/reconciler_test.go</files>
  <behavior>
    - Test 1: config override domain/domains/root/port/scheme become the published route metadata when labels are absent.
    - Test 2: label port and scheme override the config values for the same service, per D-03.
    - Test 3: merged override metadata is what `routing.GenerateDomains` consumes, so custom hostnames appear in the snapshot per D-01 and D-02.
  </behavior>
  <action>Add failing reconciler tests named around `TestReconcilerAppliesMergedOverrides...` that instantiate `ReconcilerOptions` with a project/service override map, rebuild a snapshot, and assert the winning route uses the merged hostname set, selected upstream port, and upstream scheme. Include one case where config sets `domain`, `domains`, `root`, `port`, and `scheme`, and a second case where Docker labels replace the config `port` and `scheme` per D-03.</action>
  <verify>
    <automated>go test ./internal/daemon -run 'TestReconciler(AppliesMergedOverrides|LabelOverridesConfigForPortAndScheme)'</automated>
  </verify>
  <acceptance_criteria>
    - `internal/daemon/reconciler_test.go` contains `TestReconcilerAppliesMergedOverrides`
    - `internal/daemon/reconciler_test.go` contains a label-over-config assertion for upstream port and scheme
    - The targeted `go test` command fails before implementation and passes after implementation
  </acceptance_criteria>
  <done>Reconciler tests prove snapshot routes can no longer ignore config overrides or label precedence for served hostnames and upstream target selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Merge config and label preferences before publishing snapshot routes</name>
  <read_first>
    internal/daemon/reconciler.go
    internal/daemon/reconciler_test.go
    internal/routing/overrides.go
    internal/routing/domains.go
    internal/discovery/metadata.go
    internal/discovery/ports.go
    internal/config/config.go
  </read_first>
  <files>internal/daemon/reconciler.go, internal/daemon/reconciler_test.go</files>
  <action>Extend `ReconcilerOptions` with a config override source for `config.ProjectConfig` service overrides, convert that override into `routing.RoutePreferences`, merge it with label-derived preferences via `routing.MergeOverrides`, and then use the merged values everywhere the winning route is built: pass the config port into `discovery.SelectPublishedPort`, pass merged preferences into `routing.GenerateDomains`, set `Route.Upstream.Scheme` from the merged effective scheme, and set `Route.Priority` from merged preferences when present. Keep `routeOpts` label parsing as the only source of label warnings, keep `Route.Upstream.Host` at `127.0.0.1`, and cite D-01, D-02, and D-03 in code comments only if intent would otherwise be unclear.</action>
  <verify>
    <automated>go test ./internal/daemon</automated>
  </verify>
  <acceptance_criteria>
    - `internal/daemon/reconciler.go` contains `routing.MergeOverrides(`
    - `internal/daemon/reconciler.go` contains `SelectPublishedPort(c.Ports, routeOpts, configPort)`
    - `internal/daemon/reconciler.go` passes merged preferences into `routing.GenerateDomains(`
    - `go test ./internal/daemon` exits 0
  </acceptance_criteria>
  <done>Published routes carry the effective hostname, port, scheme, and priority chosen from config+label precedence, unblocking correct HTTP and WebSocket proxy behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker/config metadata → reconciler snapshot | Untrusted label and config values become runtime routing data. |
| Snapshot route metadata → reverse proxy target | Published upstream host/port/scheme directly controls where requests are forwarded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | T | `internal/daemon/reconciler.go` | mitigate | Use `routing.MergeOverrides` so label-over-config precedence is explicit and deterministic instead of ad hoc per field. |
| T-02-06-02 | E | `internal/daemon/reconciler.go` | mitigate | Keep upstream host fixed to `127.0.0.1` and only vary port/scheme from validated metadata so hostile labels cannot redirect traffic off-loopback. |
| T-02-06-03 | R | `internal/daemon/reconciler_test.go` | mitigate | Add regression tests for config-only and label-over-config cases so future changes cannot silently reintroduce incomplete routing metadata. |
</threat_model>

<verification>
Run `go test ./internal/daemon` and confirm the new override-focused tests pass alongside existing reconciler coverage.
</verification>

<success_criteria>
- Snapshot routes honor config overrides when labels are absent.
- Docker labels still win over config for overlapping port and scheme fields.
- HTTP and WebSocket proxy handlers receive complete upstream metadata from `routing.Snapshot` without recomputing route intent.
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-dns-proxy-and-https-serving/02-local-dns-proxy-and-https-serving-06-SUMMARY.md`
</output>
