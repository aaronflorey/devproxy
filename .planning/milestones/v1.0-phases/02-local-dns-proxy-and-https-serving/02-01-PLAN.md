---
phase: 02-local-dns-proxy-and-https-serving
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/routing/types.go
  - internal/daemon/reconciler.go
  - internal/daemon/reconciler_test.go
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
requirements:
  - NET-02
  - NET-04
  - NET-05
  - NET-08
must_haves:
  truths:
    - "The published snapshot carries the effective upstream host, port, scheme, and served hostname inventory for winning routes."
    - "Runtime pause state is explicit daemon state and does not clear DNS-visible route data."
    - "HTTP redirect policy defaults remain off unless config or route data opt in later."
  artifacts:
    - path: "internal/routing/types.go"
      provides: "served-host, upstream, and listener-facing routing contracts"
    - path: "internal/daemon/reconciler.go"
      provides: "snapshot build inputs for proxy and certificate consumers"
    - path: "internal/config/config.go"
      provides: "network serving defaults including managed suffix and redirect defaults"
  key_links:
    - from: "internal/daemon/reconciler.go"
      to: "internal/routing/types.go"
      via: "snapshot route construction"
      pattern: "Upstream"
    - from: "internal/config/config.go"
      to: "internal/daemon/reconciler.go"
      via: "runtime serving defaults"
      pattern: "redirect"
---

<objective>
Extend the Phase 1 route snapshot into the authoritative serving contract for DNS, proxy, and certificate work.

Purpose: prevent the networking plans from inventing side-channel state or recalculating upstream decisions on the request path.
Output: tested snapshot/runtime types for effective upstreams, served hostnames, and paused-routing state.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-CONTEXT.md
@.planning/phases/02-local-dns-proxy-and-https-serving/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for serving-state contracts</name>
  <files>internal/daemon/reconciler_test.go, internal/config/config_test.go</files>
  <read_first>internal/daemon/reconciler.go, internal/routing/types.go, internal/config/config.go, .planning/phases/02-local-dns-proxy-and-https-serving/02-CONTEXT.md</read_first>
  <action>Add table-driven tests that prove reconciliation populates `routing.Route.Upstream` from the selected published port plus effective scheme metadata rather than the current hard-coded HTTP defaults, that winning routes expose the served hostnames needed by later certificate inventory logic, and that config defaults keep HTTP-to-HTTPS redirect disabled unless explicitly enabled. Add a daemon-state test that models paused routing as separate runtime state so a pause toggles request behavior without removing snapshot routes.</action>
  <verify>
    <automated>go test ./internal/daemon/... ./internal/config/... -run 'Test(Reconciler|DefaultConfig)'</automated>
  </verify>
  <acceptance_criteria>reconciler tests assert non-hard-coded upstream metadata and served-host inventory; config tests assert redirect remains disabled by default; at least one test proves paused state does not delete snapshot routes.</acceptance_criteria>
  <done>RED coverage exists for the serving contract that all later Phase 2 plans depend on.</done>
</task>

<task type="auto">
  <name>Task 2: Implement serving-state extensions in routing, config, and reconciler</name>
  <files>internal/routing/types.go, internal/daemon/reconciler.go, internal/config/config.go</files>
  <read_first>internal/routing/types.go, internal/daemon/reconciler.go, internal/config/config.go, internal/discovery/metadata.go, internal/discovery/ports.go</read_first>
  <action>Extend `routing` contracts so a route can carry the effective upstream scheme and host binding selected during reconciliation, plus any route-level HTTPS or redirect flags needed later by listener code. Update `internal/config/config.go` to expose typed network-serving defaults for managed suffix handling and redirect-disabled-by-default behavior. Refactor `internal/daemon/reconciler.go` so `RebuildSnapshot` builds winner routes with effective upstream metadata from existing port and label precedence logic, and add explicit daemon runtime state for paused routing that listeners can read without mutating `routing.Snapshot`.</action>
  <verify>
    <automated>go test ./internal/daemon/... ./internal/config/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>routing types expose effective upstream fields beyond host/port; reconciler no longer hard-codes upstream scheme for every route; config contains redirect-disabled defaults; tests for serving-state contracts pass.</acceptance_criteria>
  <done>Phase 2 has one authoritative serving contract for DNS, proxy, certificates, and pause handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reconciled route metadata → network listeners | Listener behavior will trust published route and pause state to decide what traffic is served. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | internal/daemon/reconciler.go | mitigate | Compute effective upstream metadata once during reconciliation so request handlers cannot diverge from label/config precedence. |
| T-02-02 | D | internal/routing/types.go | mitigate | Keep paused-routing state outside the snapshot so pause/unpause does not destroy recoverable route data. |
| T-02-03 | R | internal/config/config.go | mitigate | Encode redirect-disabled defaults explicitly so later listeners do not silently change local traffic semantics. |
</threat_model>

<verification>
- `go test ./internal/daemon/... ./internal/config/...`
- `go test ./...`
</verification>

<success_criteria>
- The snapshot and runtime contracts can drive later DNS, proxy, and cert logic without re-inspecting Docker.
- Redirect policy and pause behavior are encoded explicitly rather than inferred ad hoc.
</success_criteria>

<output>
After completion, create `.planning/phases/02-local-dns-proxy-and-https-serving/02-local-dns-proxy-and-https-serving-01-SUMMARY.md`
</output>
