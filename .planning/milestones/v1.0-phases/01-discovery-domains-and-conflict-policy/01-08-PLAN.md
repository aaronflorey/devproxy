---
phase: 01-discovery-domains-and-conflict-policy
plan: 08
type: execute
wave: 5
depends_on:
  - 01-05
files_modified:
  - cmd/devproxy/status.go
  - cmd/devproxy/doctor.go
  - cmd/devproxy/status_test.go
  - cmd/devproxy/doctor_test.go
  - internal/admin/status.go
  - internal/admin/status_test.go
autonomous: true
gap_closure: true
requirements:
  - DISC-06
must_haves:
  truths:
    - "Per 01-VERIFICATION gap 3, the `status` command exposes snapshot conflict and warning detail from the shared admin projection instead of counts only."
    - "Per 01-VERIFICATION gap 3, the `doctor` command consumes the shared `/doctor` projection so snapshot warnings and conflicts appear on the doctor surface alongside runtime checks."
    - "Status, doctor, dashboard, and logs all derive conflict/warning truth from the same snapshot-backed admin projections rather than reimplementing conflict visibility per surface."
  artifacts:
    - path: "internal/admin/status.go"
      provides: "status projection fields for reusable conflict and warning detail"
    - path: "cmd/devproxy/status.go"
      provides: "operator-visible status output that includes losing candidates and warning/conflict detail"
    - path: "cmd/devproxy/doctor.go"
      provides: "doctor command wiring that merges runtime diagnostics with the admin doctor projection"
    - path: "cmd/devproxy/status_test.go"
      provides: "CLI regression coverage for status conflict/warning rendering"
    - path: "cmd/devproxy/doctor_test.go"
      provides: "CLI regression coverage proving doctor reads the shared admin doctor view"
  key_links:
    - from: "cmd/devproxy/status.go"
      to: "internal/admin/status.go"
      via: "client.Status() returns reusable conflict/warning detail"
      pattern: "Warnings|Conflicts"
    - from: "cmd/devproxy/doctor.go"
      to: "internal/admin/doctor.go"
      via: "client.Doctor() surfaces snapshot warnings/conflicts through the doctor command"
      pattern: "Doctor()"
---

<objective>
Close the remaining Phase 1 visibility blocker from 01-VERIFICATION.md.

Purpose: make `status` and `doctor` show the same conflict and warning truth already carried by the shared snapshot, instead of hiding it behind aggregate counts or bypassing the admin doctor projection.
Output: richer status projection, doctor wiring to `/doctor`, and regression tests proving consistent user-visible output.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-CONTEXT.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-VERIFICATION.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-05-SUMMARY.md
@cmd/devproxy/status.go
@cmd/devproxy/doctor.go
@internal/admin/status.go
@internal/admin/doctor.go
@internal/admin/routes.go
@internal/admin/logs.go

<interfaces>
From `internal/admin/status.go`:

```go
type StatusView struct {
	SnapshotVersion  string
	ActiveRoutes     int
	Conflicts        int
	Warnings         int
	LastSync         time.Time
	Watcher          WatcherHealth
	DNS              DNSStatus
	HTTP             ListenerStatus
	HTTPS            ListenerStatus
	Paused           bool
	CertificateReady bool
	StartupRoles     []StartupRoleStatus
}

func BuildStatus(snapshot routing.Snapshot, watcher WatcherHealth, lastSync time.Time, runtime NetworkRuntimeStatus) StatusView
```

From `cmd/devproxy/status.go`:

```go
func newStatusCommand() *cobra.Command
```

From `cmd/devproxy/doctor.go`:

```go
func newDoctorCommand() *cobra.Command
```

From `internal/adminapi/client.go`:

```go
func (c *Client) Status(ctx context.Context) (admin.StatusView, error)
func (c *Client) Doctor(ctx context.Context) (admin.DoctorView, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock status and doctor conflict-visibility regressions in tests</name>
  <files>cmd/devproxy/status_test.go, cmd/devproxy/doctor_test.go, internal/admin/status_test.go</files>
  <read_first>cmd/devproxy/status.go, cmd/devproxy/doctor.go, internal/admin/status.go, internal/admin/doctor.go, .planning/phases/01-discovery-domains-and-conflict-policy/01-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: `BuildStatus()` exposes reusable warning and conflict detail, including losing container names or equivalent conflict records, instead of counts only.
    - Test 2: `devproxy status` prints or otherwise exposes per-conflict and per-warning detail from `client.Status()` so an operator can see why a hostname lost without calling another surface.
    - Test 3: `devproxy doctor` calls `client.Doctor()` and includes snapshot warning/conflict output from the shared admin doctor projection in addition to existing runtime checks.
  </behavior>
  <action>Add regression coverage for the exact verified gap. Create new CLI tests for `status` and `doctor` using a stub admin API server or injected client behavior, and extend `internal/admin/status_test.go` so the projection layer carries real warning/conflict detail rather than only aggregate counts. Keep the assertions outcome-focused: loser names, warning messages, and conflict reasons must appear in the status/doctor surfaces.</action>
  <verify>
    <automated>go test ./cmd/devproxy/... ./internal/admin/... -run 'Test(Status|Doctor)' -count=1</automated>
  </verify>
  <acceptance_criteria>`cmd/devproxy/status_test.go` and `cmd/devproxy/doctor_test.go` exist, the tests prove doctor consumes `/doctor`, and status assertions require visible conflict/warning detail rather than counts alone.</acceptance_criteria>
  <done>RED coverage exists for the Phase 1 cross-surface visibility gap.</done>
</task>

<task type="auto">
  <name>Task 2: Implement consistent conflict and warning visibility in status and doctor</name>
  <files>cmd/devproxy/status.go, cmd/devproxy/doctor.go, cmd/devproxy/status_test.go, cmd/devproxy/doctor_test.go, internal/admin/status.go, internal/admin/status_test.go</files>
  <read_first>cmd/devproxy/status_test.go, cmd/devproxy/doctor_test.go, internal/admin/status_test.go, internal/admin/doctor.go, internal/adminapi/client.go</read_first>
  <action>Extend `internal/admin/status.go` so `StatusView` carries reusable conflict and warning detail from the shared snapshot in a serialization-friendly form. Then update `cmd/devproxy/status.go` to render that detail clearly after the summary line, including hostnames, conflict reasons, loser container names, and warning messages. Update `cmd/devproxy/doctor.go` so it keeps the existing runtime checker behavior but also calls `client.Doctor()` and renders the snapshot-backed warnings/conflicts from the shared doctor projection instead of bypassing it entirely. Do not recompute conflict policy in either CLI command; consume the admin projections verbatim so status, doctor, dashboard, and logs stay aligned on one source of truth.</action>
  <verify>
    <automated>go test ./cmd/devproxy/... ./internal/admin/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`StatusView` contains reusable warning/conflict detail, `devproxy status` and `devproxy doctor` expose that shared information to operators, and targeted plus full tests pass.</acceptance_criteria>
  <done>DISC-06 is satisfied across status, doctor, dashboard, and logs from the same snapshot-backed projections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot-backed admin projections → CLI output | Operators trust command output to explain routing winners, losers, and warnings accurately. |
| doctor runtime checks → shared snapshot warnings/conflicts | The doctor command combines runtime health with projection data and must not drift into separate conflict logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-22 | R | `cmd/devproxy/status.go` | mitigate | Render snapshot-derived conflict/warning detail directly so operator-visible status can explain winner/loser outcomes instead of only counts. |
| T-01-23 | R | `cmd/devproxy/doctor.go` | mitigate | Call `client.Doctor()` and reuse the shared admin doctor projection so doctor cannot diverge from dashboard/logs conflict visibility. |
| T-01-24 | I | `internal/admin/status.go` | accept | Conflict and warning data are intentionally local operator-visible diagnostics; this plan broadens explanation, not exposure scope. |
</threat_model>

<verification>
- `go test ./cmd/devproxy/... ./internal/admin/... -run 'Test(Status|Doctor)' -count=1`
- `go test ./cmd/devproxy/... ./internal/admin/...`
- `go test ./...`
</verification>

<success_criteria>
- `status` and `doctor` expose the same conflict and warning truth already present in snapshot-backed admin projections.
- CLI code consumes shared projections instead of recomputing routing conflict logic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-08-SUMMARY.md`
</output>
