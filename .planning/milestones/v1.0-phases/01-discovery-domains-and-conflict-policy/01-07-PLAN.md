---
phase: 01-discovery-domains-and-conflict-policy
plan: 07
type: execute
wave: 5
depends_on:
  - 01-03
files_modified:
  - internal/routing/domains.go
  - internal/routing/domains_test.go
  - internal/daemon/reconciler_test.go
autonomous: true
gap_closure: true
requirements:
  - DOMN-04
  - DOMN-05
must_haves:
  truths:
    - "Per 01-VERIFICATION gap 2, explicit `root=false` from config or Docker labels overrides default root-service mapping instead of being ignored downstream."
    - "Root precedence remains field-level: explicit true forces root mapping, explicit false suppresses it, and the default root-service list applies only when no explicit root preference is present."
    - "The precedence rule is proven both in pure routing tests and in reconciler-level coverage so a future merge change cannot silently re-break it."
  artifacts:
    - path: "internal/routing/domains.go"
      provides: "domain-generation logic that honors explicit root preference before default root-service heuristics"
    - path: "internal/routing/domains_test.go"
      provides: "pure-function regression coverage for explicit root=false precedence"
    - path: "internal/daemon/reconciler_test.go"
      provides: "integration-level proof that merged config/label root=false suppresses root-domain publication"
  key_links:
    - from: "internal/routing/overrides.go"
      to: "internal/routing/domains.go"
      via: "merged RoutePreferences.Root drives final root-domain decision"
      pattern: "Root"
    - from: "internal/daemon/reconciler.go"
      to: "internal/routing/domains.go"
      via: "effective merged preferences feed final hostname generation"
      pattern: "GenerateDomains"
---

<objective>
Close the Phase 1 root-precedence bug from 01-VERIFICATION.md.

Purpose: make Docker labels and config overrides actually win for the overlapping root field, including the `false` case.
Output: corrected root-domain generation logic and tests that lock the precedence rule at both routing and reconciler layers.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-CONTEXT.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-VERIFICATION.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-03-SUMMARY.md
@internal/routing/domains.go
@internal/routing/overrides.go
@internal/daemon/reconciler.go

<interfaces>
From `internal/routing/overrides.go`:

```go
type RoutePreferences struct {
	Enable   *bool
	Domain   string
	Domains  []string
	Root     *bool
	Port     *int
	Scheme   string
	Priority *int
}

func MergeOverrides(config, labels RoutePreferences) (RoutePreferences, []Warning)
```

From `internal/routing/domains.go`:

```go
type RouteOptions struct {
	Suffix       string
	RootServices []string
}

func GenerateDomains(project, service string, prefs RoutePreferences, opts RouteOptions) ([]string, []Warning)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock explicit root=false precedence in tests</name>
  <files>internal/routing/domains_test.go, internal/daemon/reconciler_test.go</files>
  <read_first>internal/routing/domains.go, internal/routing/overrides.go, internal/daemon/reconciler.go, .planning/phases/01-discovery-domains-and-conflict-policy/01-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: when `service` is in the default `RootServices` list and `prefs.Root` is explicitly `false`, `GenerateDomains()` returns the default `{service}.{project}.{suffix}` hostname and does not emit `{project}.{suffix}`.
    - Test 2: when `prefs.Root` is explicitly `true`, root-domain generation still wins even if the service is not in the default root list.
    - Test 3: at reconciler level, a config or label override of `root=false` suppresses root-domain publication for a service that would otherwise map to `{project}.{suffix}` by default.
  </behavior>
  <action>Add regression tests for the exact precedence hole described in 01-VERIFICATION. Extend `internal/routing/domains_test.go` with explicit `root=false` and `root=true` cases, then add one reconciler-level test proving the merged override path suppresses publication of the root hostname when `RootServices` would otherwise generate it. Keep the assertions exact about which hostnames are present and absent.</action>
  <verify>
    <automated>go test ./internal/routing/... ./internal/daemon/... -run 'Test(DomainGeneration|Reconciler)' -count=1</automated>
  </verify>
  <acceptance_criteria>The new tests fail against the current `GenerateDomains()` behavior because explicit `root=false` is ignored today, and they explicitly cover both pure routing and reconciler integration.</acceptance_criteria>
  <done>RED coverage exists for the verified root-precedence bug.</done>
</task>

<task type="auto">
  <name>Task 2: Implement explicit root=false precedence in domain generation</name>
  <files>internal/routing/domains.go, internal/routing/domains_test.go, internal/daemon/reconciler_test.go</files>
  <read_first>internal/routing/domains_test.go, internal/daemon/reconciler_test.go, internal/routing/overrides.go</read_first>
  <action>Update `internal/routing/domains.go` so root-domain generation checks `prefs.Root` first: explicit `true` forces `{project}.{suffix}`, explicit `false` suppresses default root-service mapping, and only `nil` falls back to the configured `RootServices` list. Keep the rest of the domain behavior unchanged, including default hostnames, Sail defaults, explicit domains, and unmanaged/public suffix handling. Do not add a second root-policy type unless it is required; the existing pointer semantics already encode unset vs explicit false.</action>
  <verify>
    <automated>go test ./internal/routing/... ./internal/daemon/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`GenerateDomains()` honors explicit `root=false`, the reconciler stops publishing root domains in the override-false case, and targeted plus full Go tests pass.</acceptance_criteria>
  <done>DOMN-04 and DOMN-05 root precedence now match the Phase 1 contract instead of only the explicit-true path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config + Docker labels → final hostname set | Local override inputs determine whether a project root hostname is claimed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 | T | `internal/routing/domains.go` | mitigate | Respect explicit false before default root-service heuristics so merged override semantics are not silently rewritten downstream. |
| T-01-20 | R | `internal/daemon/reconciler.go` | mitigate | Add reconciler-level tests proving the merged root decision changes published hostnames exactly as intended. |
| T-01-21 | I | `internal/routing/domains.go` | accept | Root hostnames are intentionally local operator-visible route claims; this plan narrows claims rather than expanding them. |
</threat_model>

<verification>
- `go test ./internal/routing/... ./internal/daemon/... -run 'Test(DomainGeneration|Reconciler)' -count=1`
- `go test ./internal/routing/... ./internal/daemon/...`
- `go test ./...`
</verification>

<success_criteria>
- Explicit `root=false` suppresses default root-service mapping.
- Root precedence is locked in tests at both pure routing and reconciler levels.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-07-SUMMARY.md`
</output>
