---
phase: 01-discovery-domains-and-conflict-policy
plan: 02
type: tdd
wave: 2
depends_on:
  - 01
files_modified:
  - internal/discovery/metadata.go
  - internal/discovery/eligibility.go
  - internal/discovery/ports.go
  - internal/discovery/metadata_test.go
  - internal/discovery/eligibility_test.go
  - internal/discovery/ports_test.go
autonomous: true
requirements:
  - DISC-01
  - DISC-03
  - DISC-04
  - DOMN-04
  - DOMN-07
must_haves:
  truths:
    - "Running containers with usable published TCP ports can be normalized into route candidates on startup and refresh."
    - "Compose labels are the primary metadata source, with container-name parsing used only as fallback."
    - "Invalid override fields are ignored field-by-field while preserving the rest of the candidate."
  artifacts:
    - path: "internal/discovery/metadata.go"
      provides: "Compose metadata extraction and fallback parsing"
    - path: "internal/discovery/eligibility.go"
      provides: "Eligibility rules for routable candidates"
    - path: "internal/discovery/ports.go"
      provides: "Published-port selection precedence"
  key_links:
    - from: "internal/discovery/metadata.go"
      to: "internal/routing/types.go"
      via: "candidate construction"
      pattern: "Candidate"
    - from: "internal/discovery/ports.go"
      to: "internal/discovery/eligibility.go"
      via: "selected published port feeds routability"
      pattern: "published"
---

<objective>
Build the test-first discovery logic that turns Docker inspect data into valid route candidates.

Purpose: make startup scans and manual refresh deterministic before live watcher wiring begins.
Output: tested metadata extraction, eligibility rules, and port-selection logic.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PRD.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-RESEARCH.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for discovery normalization</name>
  <files>internal/discovery/metadata_test.go, internal/discovery/eligibility_test.go, internal/discovery/ports_test.go</files>
  <read_first>internal/routing/types.go, internal/config/config.go, .planning/PRD.md</read_first>
  <action>Create table-driven tests that describe the required behaviors: use `com.docker.compose.project` and `com.docker.compose.service` first; fall back to container-name parsing only when those labels are absent; treat only running containers with at least one published TCP port as routable; honor `devproxy.enable=true` over ignore lists but not over the hard published-port requirement; and choose the upstream host port using the exact PRD precedence (`devproxy.port`, config override, 443, 8443, 80, 8080, 8000, 3000, 5173, 8025, then first published TCP port). Include invalid label cases proving malformed priorities/domains/ports are ignored field-by-field with warnings rather than crashing candidate creation.</action>
  <verify>
    <automated>go test ./internal/discovery/... -run 'Test(ComposeMetadata|Eligibility|PortSelection)'</automated>
  </verify>
  <acceptance_criteria>The discovery test files contain cases for label-first metadata, fallback parsing, ignore-list override behavior, invalid-label warning behavior, and full port precedence from the PRD.</acceptance_criteria>
  <done>RED tests exist for every core discovery rule in DISC-01/03/04 and DOMN-04/07.</done>
</task>

<task type="auto">
  <name>Task 2: Implement metadata extraction, eligibility, and port selection</name>
  <files>internal/discovery/metadata.go, internal/discovery/eligibility.go, internal/discovery/ports.go</files>
  <read_first>internal/discovery/metadata_test.go, internal/discovery/eligibility_test.go, internal/discovery/ports_test.go, internal/routing/types.go</read_first>
  <action>Implement typed helpers that accept Docker inspect/list data and return normalized route candidates plus warning records. `metadata.go` must prefer Compose labels and only parse container names as fallback; `eligibility.go` must enforce running-state + published-TCP-port + disable/ignore rules; `ports.go` must select a published host port using the exact precedence from the PRD and preserve the selected host binding for later routing. When label values are malformed, ignore only the bad field and attach a warning record that downstream status/doctor/log surfaces can render per DOMN-07.</action>
  <verify>
    <automated>go test ./internal/discovery/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>Discovery packages expose pure functions returning candidates and warning records; tests for metadata, eligibility, and port selection pass; malformed label fields do not abort the candidate when other fields remain valid.</acceptance_criteria>
  <done>Phase 1 has deterministic startup/refresh candidate generation logic ready for registry reconciliation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker metadata → discovery logic | Container labels and names are untrusted inputs that can attempt to influence routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | S | internal/discovery/metadata.go | mitigate | Prefer canonical Compose labels and only use fallback name parsing when labels are absent, reducing spoofing by ambiguous names. |
| T-01-05 | T | internal/discovery/ports.go | mitigate | Validate `devproxy.port` against actual published host ports and ignore non-matching values with warnings. |
| T-01-06 | D | internal/discovery/eligibility.go | mitigate | Reject non-running or non-published containers early so bogus candidates never enter reconciliation. |
</threat_model>

<verification>
- `go test ./internal/discovery/...`
- `go test ./...`
</verification>

<success_criteria>
- Discovery logic is test-driven and deterministic.
- Invalid label fields degrade to warnings instead of daemon-breaking failures.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-02-SUMMARY.md`
</output>
