---
phase: 01-discovery-domains-and-conflict-policy
plan: 06
type: execute
wave: 5
depends_on:
  - 01-05
files_modified:
  - internal/daemon/app.go
  - internal/daemon/events.go
  - internal/daemon/docker_runtime.go
  - internal/daemon/app_test.go
  - internal/daemon/reconciler_test.go
  - internal/daemon/events_test.go
autonomous: true
gap_closure: true
requirements:
  - DISC-02
must_haves:
  truths:
    - "Per 01-VERIFICATION gap 1, route mappings update automatically when Compose containers start, stop, die, destroy, rename, or update, instead of only during startup and manual refresh scans."
    - "Watcher disconnect and reconnect state comes from the real Docker lifecycle stream, and reconnect forces a full resync before the watcher reports healthy again."
    - "The live event path reuses the existing reconciler snapshot pipeline instead of inventing a second route-update code path."
  artifacts:
    - path: "internal/daemon/events.go"
      provides: "real Docker event subscription, event filtering, and reconnect/full-resync watcher orchestration"
    - path: "internal/daemon/app.go"
      provides: "daemon startup wiring that runs the Docker event loop alongside startup refresh"
    - path: "internal/daemon/docker_runtime.go"
      provides: "default Docker event source implementation using the existing local Docker CLI/runtime approach"
  key_links:
    - from: "internal/daemon/app.go"
      to: "internal/daemon/events.go"
      via: "Start() launches live watcher loop after initial refresh"
      pattern: "watch|events|reconnect"
    - from: "internal/daemon/events.go"
      to: "internal/daemon/reconciler.go"
      via: "supported lifecycle events call HandleEvent/RebuildSnapshot"
      pattern: "start|stop|die|destroy|rename|update"
---

<objective>
Close the Phase 1 live-update blocker from 01-VERIFICATION.md.

Purpose: make route state track the real Docker lifecycle continuously instead of only during startup and manual refresh.
Output: event-stream subscription, reconnect-aware watcher health, and regression tests proving automatic route updates.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-CONTEXT.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-VERIFICATION.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-05-SUMMARY.md
@internal/daemon/app.go
@internal/daemon/events.go
@internal/daemon/docker_runtime.go
@internal/daemon/reconciler.go

<interfaces>
From `internal/daemon/app.go`:

```go
type AppConfig struct {
	AdminSocketPath     string
	DashboardAddress    string
	HTTPAddress         string
	HTTPSAddress        string
	Config              config.Config
	DockerPing          func(context.Context) error
	DockerScan          func(context.Context) ([]ContainerState, error)
	EnsureMKCert        func(context.Context) error
	BuildNetworkRuntime func(context.Context) error
}

func (a *App) Start(ctx context.Context) error
func (a *App) Refresh(ctx context.Context) error
```

From `internal/daemon/reconciler.go`:

```go
func (r *Reconciler) RebuildSnapshot(containers []ContainerState) error
func (r *Reconciler) HandleEvent(event string, containers []ContainerState) error
func (r *Reconciler) Snapshot() routing.Snapshot
```

From `internal/daemon/events.go`:

```go
type Watcher struct {
	reconciler *Reconciler
	health     WatcherHealth
}

func NewWatcher(reconciler *Reconciler) *Watcher
func (w *Watcher) OnDisconnect()
func (w *Watcher) OnReconnect(containers []ContainerState)
func (w *Watcher) Health() WatcherHealth
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock real Docker event lifecycle behavior in tests</name>
  <files>internal/daemon/app_test.go, internal/daemon/reconciler_test.go, internal/daemon/events_test.go</files>
  <read_first>internal/daemon/app.go, internal/daemon/events.go, internal/daemon/reconciler.go, .planning/phases/01-discovery-domains-and-conflict-policy/01-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: after startup refresh succeeds, a supported lifecycle event (`start`, `stop`, `die`, `destroy`, `rename`, or `update`) delivered through the watcher path triggers a fresh snapshot rebuild without requiring `Refresh()`.
    - Test 2: watcher disconnect marks degraded health immediately, then reconnect performs one full resync before `Connected=true` and `LastReconnectSync` update.
    - Test 3: unsupported or malformed Docker events do not mutate the snapshot and are ignored or surfaced as non-fatal watcher issues instead of crashing the daemon loop.
  </behavior>
  <action>Add focused tests that reproduce the actual missing behavior from 01-VERIFICATION. Replace the current manual `OnDisconnect()`/`OnReconnect()`-only coverage with event-loop-level tests that prove `App.Start()` wires a long-lived watcher source, supported Docker lifecycle events cause reconciliation, and reconnect health only flips back to healthy after a full scan succeeds. Keep the tests dependency-injected; do not shell out to Docker in unit tests.</action>
  <verify>
    <automated>go test ./internal/daemon/... -run 'Test(App|Watcher|Reconciler)' -count=1</automated>
  </verify>
  <acceptance_criteria>`internal/daemon/events_test.go` exists, startup-plus-live-event behavior is covered by tests, and reconnect/full-resync semantics are pinned without relying on manual helper calls alone.</acceptance_criteria>
  <done>RED coverage exists for the actual Phase 1 gap: no live Docker event subscription.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Docker event subscription and reconnect-driven full resync</name>
  <files>internal/daemon/app.go, internal/daemon/events.go, internal/daemon/docker_runtime.go, internal/daemon/app_test.go, internal/daemon/reconciler_test.go, internal/daemon/events_test.go</files>
  <read_first>internal/daemon/events_test.go, internal/daemon/app_test.go, internal/daemon/docker_runtime.go, internal/daemon/reconciler.go</read_first>
  <action>Introduce a real event-source abstraction in the daemon layer and wire it into `App.Start()` after the initial refresh succeeds. Stay on the existing Docker-runtime approach already used in `DefaultDockerPing()` and `DefaultDockerScan()`: add a default implementation in `internal/daemon/docker_runtime.go` that streams `docker events` output, filters to the supported lifecycle actions (`start`, `stop`, `die`, `destroy`, `rename`, `update`), and asks the watcher/reconciler path to refresh snapshot state from current containers. On stream failure or EOF, mark watcher health disconnected, record a daemon issue, retry connection, and force a full resync before reporting reconnect health. Keep one authoritative snapshot path by routing both event-triggered updates and reconnect recovery through the reconciler instead of mutating snapshot maps directly.</action>
  <verify>
    <automated>go test ./internal/daemon/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>`internal/daemon/app.go` starts a real event loop, `internal/daemon/events.go` handles disconnect/reconnect and supported lifecycle actions, `internal/daemon/docker_runtime.go` provides the default event source, and targeted plus full tests pass.</acceptance_criteria>
  <done>Phase 1 route state now stays current from the live Docker lifecycle, satisfying DISC-02 instead of only startup/manual refresh scans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker event stream → daemon snapshot state | External lifecycle data continuously influences the active route registry. |
| watcher health → operator surfaces | Status and diagnostics depend on accurate disconnect/reconnect truth from the live watcher path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | D | `internal/daemon/events.go` | mitigate | Detect event-stream disconnects, mark degraded watcher health immediately, and require a successful full resync before reporting reconnect success. |
| T-01-17 | T | `internal/daemon/reconciler.go` | mitigate | Route every live update through the existing rebuild path so event ordering cannot mutate snapshot state in ad hoc ways. |
| T-01-18 | R | `internal/daemon/app.go` | mitigate | Record watcher disconnect and resync failures as session issues so operator surfaces can explain stale-route risk explicitly. |
</threat_model>

<verification>
- `go test ./internal/daemon/... -run 'Test(App|Watcher|Reconciler)' -count=1`
- `go test ./internal/daemon/...`
- `go test ./...`
</verification>

<success_criteria>
- Route state updates automatically from live Docker lifecycle events.
- Watcher disconnect/reconnect truth is driven by the real event stream and forces a full resync on recovery.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-06-SUMMARY.md`
</output>
