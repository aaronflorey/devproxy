---
phase: 01-discovery-domains-and-conflict-policy
plan: 04
type: tdd
wave: 3
depends_on:
  - 02
  - 03
files_modified:
  - internal/routing/conflicts.go
  - internal/registry/snapshot.go
  - internal/routing/conflicts_test.go
  - internal/registry/snapshot_test.go
autonomous: true
requirements:
  - DISC-05
  - DISC-06
must_haves:
  truths:
    - "When multiple candidates claim the same domain, the winner is chosen by priority then stable container-name tie-break."
    - "Losing candidates and warning reasons are preserved in a read model reusable by status, doctor, logs, and dashboard consumers."
  artifacts:
    - path: "internal/routing/conflicts.go"
      provides: "Deterministic conflict-resolution algorithm"
    - path: "internal/registry/snapshot.go"
      provides: "Immutable route snapshot with conflicts and warnings"
  key_links:
    - from: "internal/routing/conflicts.go"
      to: "internal/registry/snapshot.go"
      via: "resolved winners and losers are published into the active snapshot"
      pattern: "Conflict"
---

<objective>
Convert normalized candidates into one authoritative active-route snapshot with deterministic conflict outcomes.

Purpose: make every later surface read the same answer about winners, losers, and warnings.
Output: tested conflict-resolution logic and immutable registry snapshot builder.
</objective>

<execution_context>
@/home/aaron/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/aaron/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PRD.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-RESEARCH.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-02-PLAN.md
@.planning/phases/01-discovery-domains-and-conflict-policy/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for conflict resolution and snapshot publication</name>
  <files>internal/routing/conflicts_test.go, internal/registry/snapshot_test.go</files>
  <read_first>internal/routing/types.go, internal/discovery/metadata_test.go, internal/routing/domains_test.go</read_first>
  <action>Write tests that prove domain claims are grouped by hostname, highest numeric priority wins, and equal priorities are resolved by stable sort on container name. Include cases proving the read model preserves the winning route, all losing candidates, conflict reason strings, warning records, and the effective hostname set for a snapshot version so downstream status/doctor/log consumers cannot recompute a different answer.</action>
  <verify>
    <automated>go test ./internal/routing/... ./internal/registry/... -run 'Test(ConflictResolution|RegistrySnapshot)'</automated>
  </verify>
  <acceptance_criteria>Conflict and snapshot test files assert priority wins, stable tie-break, loser retention, and shared warning/conflict publication.</acceptance_criteria>
  <done>RED tests lock down deterministic conflict behavior and shared read-model publication.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic conflict policy and immutable snapshot builder</name>
  <files>internal/routing/conflicts.go, internal/registry/snapshot.go</files>
  <read_first>internal/routing/conflicts_test.go, internal/registry/snapshot_test.go, internal/routing/types.go</read_first>
  <action>Implement a pure conflict resolver that accepts resolved route candidates, groups them by hostname, selects the winner by priority then stable container-name ordering, and emits explicit loser metadata and human-readable reasons. Implement `internal/registry/snapshot.go` to assemble an immutable snapshot containing active routes by hostname, conflicts, warnings, timestamps, and a version identifier so later watchers can swap the whole state atomically rather than mutating maps in place.</action>
  <verify>
    <automated>go test ./internal/routing/... ./internal/registry/... && go test ./...</automated>
  </verify>
  <acceptance_criteria>Snapshot builder exposes one authoritative structure with active routes, conflict losers, warnings, version, and timestamps; conflict tests and registry tests pass.</acceptance_criteria>
  <done>Phase 1 has deterministic route resolution and a copy-on-write registry foundation for all downstream readers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Candidate routes → active registry | Multiple untrusted container candidates compete for the same hostname. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | T | internal/routing/conflicts.go | mitigate | Enforce a single deterministic winner function (priority then container-name sort) instead of event-order-dependent mutation. |
| T-01-11 | R | internal/registry/snapshot.go | mitigate | Persist loser metadata and reasons in the snapshot so operator surfaces can explain every decision. |
| T-01-12 | D | internal/registry/snapshot.go | mitigate | Use immutable snapshot replacement to avoid partial state during concurrent reads. |
</threat_model>

<verification>
- `go test ./internal/routing/... ./internal/registry/...`
- `go test ./...`
</verification>

<success_criteria>
- Conflict outcomes are deterministic and explainable.
- One immutable registry snapshot exists for every user-facing surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-domains-and-conflict-policy/01-discovery-domains-and-conflict-policy-04-SUMMARY.md`
</output>
